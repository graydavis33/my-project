# Current Priorities

_Last updated: 2026-06-03 — **Populated the Sandcastles watchlist (continuation of the 6/02 MCP integration).** Reloaded + OAuth'd the `sandcastles` MCP live (tools: top_topics/top_hooks/top_formats/search_all_videos/search_my_videos/discover_channels/add_channels_to_watchlist/channel_recap/analyze_video). **Pro = ONE workspace** (no create-workspace tool; multi-workspace is Titan-only) → built a single Graydient watchlist; Sai's research goes through global tools (`search_all_videos` + channel-UUID filter), not a 2nd list. **Used real Sandcastles outlier data to answer Gray's "3 videos to make next" question** (top picks: "Result vs BTS" effect reveal backed by khan.ayan9's 265x; AI-tools workflow listicle backed by dope.motions' 35M-view outlier; "what AI can do in 2026" capability-reveal). **Key strategic pivot — the watchlist hard gate is PRODUCTION-FIT, not follower-size or niche:** Gray's energy goes to Sai (Graydient = leftover-time, low-lift, same-day videos only), so studying cinematic creators is harmful (teaches unreplicable formats). Curated toward AI-tool/screen-record + talking-head editing tips + small/emerging; cut cinematic/effect-build creators. Iterated v1→v4 across ~7 discover_channels searches. Added ~27 channels incl. Gray's 3 own accounts (IG+TikTok+YT Shorts, for own-analytics study), 4 adjacent lenses (colinandsamir/itsryanto/aliabdaal/themayanknegi for different hooks/structures), standouts on Gray's exact stack (covacut = Firefly+Claude+Higgsfield; dmytro.edits; benkaluza = Claude). **The "50-channel Pro cap" is NOT hard-enforced via MCP** — watchlist sits at ~59, all adds went through. Plan/live-state doc at `business/sandcastles-watchlists.md`. **Pending:** Gray to prune cinematic pre-existing picks (blake.ridder/henbu_/qfroost/kevinbparry/prismlensfx/qmike/omgadrian/jordanagajanian) in the webapp for signal quality (removal is webapp-only, no MCP remove tool); then run video-suggest/hooks-watchlist/topics against the clean list. Earlier 2026-06-03 — **First production run of the Scriptwriter system** (story-arc-playbook + scriptwriter subagent + sai-weekly-script-batch SOP). Sai dropped 13 voice memos into a karramedia.com Drive folder `Batch Memos 2`; MCP couldn't see karramedia (auth'd to graydavis33 only), so Gray shared the folder over. Downloaded all 13 .m4a via the Drive MCP's `download_file_content` (base64 JSON dumped to tool-results temp files, batch-decoded via a single Python pass). Transcribed all 13 with local Whisper large-v3 on the RTX 5070 (~5 min wall-clock for 13 files). Merged the 2 finance-pair transcripts (recording cut off mid-thought → restarted, so "pt 1" + "pt 2" are continuations, not separate ideas) → 11 final transcripts. Scriptwriter subagent ran in its own context (1 invocation, 19 tool uses, ~135s, ~105K subagent tokens) and wrote 11 A/B/C-hook shorts scripts to `business/social-media/sai/scripts/2026-06-02-batch-2.md` matching the 5/26 Batch 1 format. **Two scripts use Sai's intentional improvised hook** (`Landed a Fortune 500 client` + `Accept your passion` — flagged by parenthetical in filenames). **2 verification flags** for Sai before filming: Script 2's Hook A came from Gray brainstorming mid-recording (Sai endorsed it, but agent contract says "Sai's actual words" — rule needs clarification), and Script 8's closer "We won." is the only invented line (transcript implies Fortune 500 win heavily but never explicitly states it). **v1 retro logged across all 3 phases** at `docs/superpowers/specs/2026-06-02-scriptwriter-v1-retro.md` with 14 v2 candidates. Top items: second gdrive MCP authed to karramedia (auth gap recurs every batch); add `--transcribe-only --all` batch support to `content-pipeline/main.py` (currently only `--meeting-notes` batches); add a list-format Haiku pre-pass for transcripts where Sai stutters list items 2-3× (Script 1 "10 truths" was the hardest to write); fix `main.py` Windows stdout encoding (cp1252 crashes on em-dashes — worked around with `PYTHONIOENCODING=utf-8`); add a parable/anecdote shorts template (Script 4 "Fire sirens" had no playbook pattern); name "Authority-Anchor Opener" as a hook variant in the style guide (both intentional-hook recordings led with dollar-scale credibility). Earlier 2026-06-02 — **Integrated Sandcastles as the 7th user-scope MCP.** Sandcastles = the short-form video research tool Gray pays for (outliers/hooks/formats/topics across IG/TikTok/YT Shorts; site sandcastles.ai). It just shipped a hosted MCP + Claude Cowork plugin + Public API. Gray had extracted the plugin zip to the footage-drive root (`D:\sandcastles-claude-plugin-v1.0.2`). Wired it in: registered `sandcastles` → `{"type":"http","url":"https://mcp.sandcastles.ai/"}` in `~/.claude.json` (backup at `~/.claude.json.bak`; VS Code `--strict-mcp-config` ignores plugin-bundled `.mcp.json`, so direct registration was required); copied the 14 slash commands to `.claude/commands/sandcastles/` (`/sandcastles:*` — live now); moved plugin source to `references/sandcastles-claude-plugin/` and cleaned D: root. Mapped the MCP tool surface (`top_topics`, `top_hooks`, `top_formats`, `search_all_videos`, `search_my_videos`, `discover_channels`, `add_channels_to_watchlist`, `channel_recap`, `analyze_video`, `list_automation_rules`). SOP at `workflows/sandcastles.md`; CLAUDE.md MCP table + memory (`reference_sandcastles_mcp.md`) updated. **Name trap noted:** "Sandcastle AI" (sandcastle-ai.eu/gizmax) is a totally different DEV orchestration tool — not this. **Public API is Titan-plan-locked** (Gray's on Pro); the MCP works on Pro. Designed two 25-channel watchlists (Pro caps ~50 total) at `business/sandcastles-watchlists.md` — Gray (videography/editing/AI) + Sai (founder/business), each: own channel + 5 macro / 10 mid / 9 nano, niche-fit as hard gate, ~5 lateral picks, short-form accounts only, skewed nano/mid for replicability. Macro tier seeded from Gray's `creator-intel` craft creators (their short-form accounts). **Open follow-ups:** Gray to reload VS Code window + OAuth to activate the MCP, then fill Mid/Nano rows live via `/sandcastles:channels-search`; needs to supply Callaway's handle + Sai's account handle. Earlier 2026-06-02 — **Diagnosed and fixed a 23-day-old lineage split that had been silently routing Windows session commits to `origin/snapshot-2026-05-29-windows` instead of `origin/main`.** A paused interactive rebase from 5/29 had parked the working dir on the snapshot (detached HEAD); `safe-session-commit.sh` pushed to whatever branch was checked out, so ~60 HyperFrames composition dirs + the 8 content-OS SOPs from the 5/10 pivot + `references/thumbnail-patterns-2026-05-18.md` + `sai-thumbnail/` + `python-scripts/sfx-library/` + `web-apps/day-x-study/` + `workflows/caption-standards.md` + `plans/2026-05-13-ep1-effects-study.md` all landed on snapshot branches and never reached origin/main. Mac side kept pushing to main normally → silent divergence. Audited which paths were unique to each surviving snapshot branch (`backup-save-2026-05-31`, `windows-batch-work-2026-05-29`, `snapshot-2026-05-29-windows`), restored **762 files** to origin/main via per-path `git checkout <branch> -- <path>` (64 from `backup-save-2026-05-31` as freshest, 4 from `snapshot-2026-05-29-windows`). Recovery committed as `987caac`. Dropped the 3 batch-1 caption layer dirs (sai-b01v04/v05/v06-captions) — Gray wanted only `workflows/caption-standards.md` (the SOP) kept long-term, not the per-video output dirs. **Plugged 2 credential leaks:** (a) `_memory/github.md` was accidentally restored from snapshot (had been intentionally removed + gitignored on main 2 commits prior in `aefbe74`); pulled it back out. (b) `python-scripts/social-media-analytics/tiktok_token.json` was tracked on main with a real (already-expired/invalid) TikTok access_token + refresh_token; existing `token*.json` gitignore pattern didn't match because the filename prefix isn't `token` — broadened to `*token*.json`, untracked the file. **Added two new auto-commit guards** to prevent recurrence: `safe-session-commit.sh` now aborts if `HEAD != main` (including detached HEAD during rebases), AND the SessionStart hook in `~/.claude/settings.json` now prints a stderr warning if the working branch isn't main. Cleaned up 3 stale snapshot branches locally + remotely (`backup-save-2026-05-31`, `windows-batch-work-2026-05-29`, `snapshot-2026-05-29-windows`) plus 2 remote-only ones (`session-2026-05-31-backup`, `session-2026-05-31-vid7`); 4 earlier merged branches `-d` deleted cleanly; only `claude/review-project-context-ULxJd` remains on remote (Claude Code review artifact, not a snapshot). **Also fixed Obsidian MCP path** — `~/.claude/settings.json` was pointing at the deleted Documents vault (`Documents/Obsidian/Graydient Media` was consolidated to Drive on 5/08 but the MCP arg never updated); repointed at `C:/Users/Gray Davis/My Drive/Obsidian/Graydient Media`. **Open follow-ups:** `web-apps/hyperframes/sai-vid3-captions/source.mp4` is 75MB on origin/main (GitHub warned, not blocking — long-term need LFS or gitignore `web-apps/**/source.mp4`); `web-apps/sai-b01v07-captions/` was untracked at the 5/31 abort per `auto-commit-skipped.log` and isn't in the recovered set (verify on Mac); `Documents/Obsidian/` orphan content (Business/, Content/, Ideas Backlog.md, etc.) still pending triage from 5/08. Earlier 2026-05-02 (later) — **Footage Organizer Windows + Mac compatibility shipped on `feature/footage-organizer-v2`.** SQLite index now stores POSIX-relative paths (`05_FOOTAGE_LIBRARY/...`) instead of machine-absolute strings, so the same `.footage-index.sqlite` file on the shared exFAT SSD works from either machine. `cli_index._check_legacy_db()` auto-detects pre-migration absolute-path DBs and wipes the clips table on first run; next index repopulates with relative paths. Symmetrical migration — works whether Mac or Windows runs the new code first. `pull.pull()` and `index.remove_missing()` now require `library_root` kwarg. Walker filters Mac AppleDouble `._*` and `.DS_Store`. Plan at `plans/2026-05-02-footage-organizer-windows-compat.md` (subagent-driven execution: 9 commits, two-stage review per task, 27/27 tests green). Live-verified on Windows during the session: legacy DB detected + wiped, 431 clips re-indexed (post-flatten growth from the 238 baseline), April 29 pull → 94 clips/19GB, April 30 pull → 26 clips, plus a one-off Vision picker `.tmp/find_visuals.py` (top-N by Haiku scoring against a text description) that revealed April 30 was actually indoor interview/Trendify office content, not the subway/blue-sweatshirt footage Gray remembered. User promoted previous frame-finder to `python-scripts/sai-linkedin/find_visuals.py` (already built for the new relative-path index — `abs_path = ROOT / rel_path`). **Mac-side verification still pending:** plug SSD in, `git pull`, `pytest tests/` should report 27/27, then `python cli_index.py --client sai index` should run with NO `! Detected legacy` warning (Windows already migrated the DB). Earlier 2026-05-02: Locked in the **manual-edit + Claude-generated-asset** workflow for Sai long-form. Gray edits in Premiere; Claude generates templated motion graphics on demand. First production run: built lesson-card animation system at `D:/Sai/08_AI_EDITS/long-form/A-Roll Long Lessons I learned from growing a business/edit/assets/lesson_cards/` — parameterized PIL+ffmpeg script (`build_lesson_card.py`) renders a 5s animated card: numeral card scales in centered, slides left while a text bar extends right with left-to-right text wipe, all on cubic ease-out. Output is ProRes 4444 .mov with alpha (numeral cutout = true transparent so footage shows through in Premiere) + a green-bg MP4 preview. Three lessons rendered: (1) "Don't take shortcuts that break trust… even if they work." (2) "Don't rely on others to scale. Don't overpromise to anyone." (3) "Obsess over your customer. Nothing else matters as much." Brand defaults: Montserrat (downloaded variable font to `.fonts/Montserrat-VF.ttf` on first need), white only, free use of chrome (Gray rejected the "minimal chrome" rule). Also generated matched green-screen number cards (1/2/3) at `Downloads/card_{1,2,3}.png` for static use. Workflow + asset spec saved in memory: `workflow_sai_video_assets.md`. Earlier 2026-05-01 (later): Re-rendered the 04-30 long-form dual-cam edit with new padding (0ms head / 400ms tail, was 50/100). Trailing-word chop fixed; tight starts. Re-render reads saved artifacts (`edit/claude_keep.json` + `words.json` + `sync.json`) — no Whisper/Claude re-pay. Locked both cams to 24000/1001 fps + 48kHz audio (A is 23.976fps, B is 25fps; without lock, 79 cuts accumulated 0.78s drift). Script: `python-scripts/multicam-mirror/longform_rerender.py` (re-runs in ~2 min). Output: `D:/Sai/AI Edits/2026-04-30/long-form-v2/final_Aroll.mp4` + `final_Broll.mp4`, both 529.59s, frame-locked to 0.25ms. 5 tail picks dropped + 1 clamped because B-cam stopped recording 54s before A. Earlier 2026-05-01: First end-to-end production run of the **LinkedIn Production System**. Trigger short = `D:/Sai/04_DELIVERED/shorts/Apr 20 – Apr 26/2026-04-23 Hook 1 Do Over V2.mp4`. Whisper large-v3 on RTX 5070 transcribed in 5 sec → `master.srt` in a fresh `D:/Sai/AI Edits/2026-04-23 Hook 1 Do Over V2/` folder. `sai-linkedin` produced caption + theme + key_points + 5 visual ideas (Sonnet 4.6, ~$0.07 with cache writes — repeat runs ~$0.005). Built a one-off frame-finder that hits the SQLite footage index (238 clips), samples 10 candidates per visual idea, sends thumbs to Haiku Vision in batched calls for 0–10 scoring, extracts HQ JPGs of winners → 5 screenshots in `linkedin/visuals/` (top match 8/10: Central Park rock shot, `idea4_score8_C2206.jpg`). Sai applied 8 distinct revisions to the AI draft — em-dashes cut, AI-essay headers replaced ("The shift that actually changed things for me:" → "So here's what I did:"), poetic verbs flattened ("breathes differently" → "feels different"), declarative claims hedged ("Now I think" → "Now I try to think"), units sharpened ("bad months / slow periods" → "bad days / slow months"), escalation symmetry fixed ("months → years" → "weeks → months"). Saved his revision to `reference/voice/sai-linkedin-posts-final.md` (cached corpus learns from real edits) AND encoded the 8 patterns as new AVOID rules in `SYSTEM_PROMPT_CORE` (~6.2K → 7.2K chars). Memory: `feedback_sai_linkedin_voice_revisions.md`. Frame-finder script lives in `.tmp/` and was deleted after use — promote to `python-scripts/sai-linkedin/find_visuals.py` if Tier-2 photo-pick is wanted. Earlier (2026-04-30): Big AI-editing session. (1) Built a 21-clip vertical day-in-life montage from `D:\Sai\02_ORGANIZED\B-roll and A-roll` — chronological by capture metadata, 4.5s per clip with windows picked by HSV-skin-exposure + frame-diff motion scoring, orange `#F28129` "X:XX pm" timestamp matching the `mastery draft 4.mp4` reference style. Output at `D:\Sai\AI Edits\2026-04-30\final.mp4` (timestamped) + `final_no_timestamps.mp4`. (2) Scaffolded **Remotion Studio** project at `web-apps/remotion-sai-vlog/` with Zod-schema-driven prop editing for clips/text/color/font-size in the right sidebar. Studio runs locally at `localhost:3000` via `npm start`. Note: Remotion's "open in editor" is blocked on Windows by the space in "Gray Davis" — must edit `.tsx` files in VS Code directly. (3) Built dual-cam long-form pipeline for `D:\Sai\A-Roll Long Lessons I learned from growing a business\` — audio cross-correlation (B started +51.57s after A), Whisper-large-v3 transcription on RTX 5070 (82s for 18 min), Claude Sonnet 4.5 picks best takes, output two synced MP4s at `D:\Sai\AI Edits\2026-04-30\long-form\final_Aroll.mp4` + `final_Broll.mp4`, both 578.78s exactly, frame-locked cuts. 18:40 → 9:39 (47% trim). (4) Resolved the paused 37-commit footage-organizer-v2 rebase — took HEAD content for all 6 conflicts; queue collapsed several duplicate "Session update" commits as already-upstream. Earlier (Mac, 2026-04-28): First production run of `video-use` AI editor on Sai 2026-04-28 footage (3:20 → 32s, ~$0.02). Footage Organizer v2 hardening — `--format` and `--top-level-only` flags added, `--source` default copy→move, `cli_index.py` skips `Adobe Premiere Pro *` subdirs (was indexing 585 fake preview clips). Final index: 238 real clips. Earlier: Started brainstorming the **LinkedIn Production System** for Sai. Trigger = Short publishes → transcribe → draft LinkedIn post in Sai's voice → pair with old footage screenshot → publish. Brainstorm paused at Question 1 of ~5 on Mac. Built `python-scripts/gdocs-cli/` and the "Karra Media Automation" Google Cloud project under gray@karramedia.com (separate from graydavis33's social-media-analytics-488803). Used it for Ari Goren recommendation letter + Trendify Props Title Pages doc._
_Update this file whenever your focus shifts._

> **⚡ MAJOR FOCUS SHIFT 2026-04-15:** Sai Karra job begins — 5h filming + 3h editing daily, 1 short/day + 1 LinkedIn/day + 1 long-form/week. Primary income + primary creative output. Debrief materials at `plans/2026-04-15-sai-debrief-content-strategy.md` (strategy), `plans/2026-04-15-sai-debrief-research-deepdive.md` (intel dump), `plans/2026-04-15-sai-footage-organization-system.md` (filing system), `plans/2026-04-16-sai-format-shooting-scripts.md` (on-set shooting scripts for every format + series). All internal-project work below secondary to Sai deliverables until stable cadence established (~Day 30).

## Scoring Framework

Each project scored 0–3 across 4 dimensions:
1. Unblocks something else
2. Direct revenue or time impact
3. Showcase / social media value
4. Builds toward a sellable product

**Tiebreaker:** simpler/faster project goes first.

---

## Active Priority List

| # | Project | Score | Status | Next Action |
|---|---------|-------|--------|-------------|
| 1 | Content Pipeline | 9/12 | LIVE on Windows GPU (CUDA 12.8). 10 voice memos transcribed 2026-04-19 → `Voice Memos/` vault folder + index. Filename date-prefix parsing + auto-backlink shipped. | For future memos: drop `.m4a` in `C:/Users/Gray Davis/My Drive/Voice Memos/` with `YYYY-MM-DD - ` prefix, run `python main.py "C:/Users/Gray Davis/My Drive/Voice Memos" --meeting-notes --all`. Upgrade: iOS Shortcut or Just Press Record app to auto-prefix dates. |
| 2 | Content Researcher | 9/12 | LIVE on Windows + Mac (agent loop + Reddit already exist) | Docs out of date — content-researcher.md describes old 5-step pipeline, actual code is agentic |
| 3 | Client Onboarding | 7/12 | Built on Windows | Fill .env, review contract template |
| — | Social Media Analytics | 9/12 | LIVE — Instagram data broken (scraper) | Meta app configured (4 permissions added) — next: get access token from Graph API Explorer, add to .env, swap main.py to meta_fetcher |
| 5 | Hook Optimizer | 6/12 | LIVE on Windows (real .env key set 2026-04-13) | Add .env on Mac |
| 6 | Creator Intel | 5/12 | Built on Windows | Needs YouTube OAuth |
| 7 | Morning Briefing | 4/12 | Built on Windows | Needs full .env setup |
| 8 | Footage Organizer | 8/12 | v2 LIVE on `feature/footage-organizer-v2`. Index has 431 clips at `D:/Sai/.footage-index.sqlite` (re-indexed 2026-05-02 with POSIX-relative paths for cross-machine compat — same SQLite now works on Mac and Windows). 9-folder library scheme: `00_TEMPLATES` … `05_FOOTAGE_LIBRARY/<cat>/W##_*` … `08_AI_EDITS/<pipeline>/<source>/`. Flags: `--format <long-form\|short-form>`, `--top-level-only`, `--source` defaults to MOVE, `--allow-today` escape hatch. Pull: `python cli_index.py --client sai pull --orientation vertical --filmed-date YYYY-MM-DD`. Cleanup: `pull-cleanup [--older-than N]`. Weekly folder creation: `create-week`. Vision picker (one-off, gitignored at `.tmp/find_visuals.py`) scores clips against a free-text description and copies top-N — proves out the "find me the subway shot of Sai" use case. | **Replug SSD into Mac and run final cross-machine verification:** `git pull && cd python-scripts/footage-organizer && pytest tests/` (expect 27/27) then `python cli_index.py --client sai index` (expect NO `! Detected legacy` warning since Windows already migrated). After that: merge `feature/footage-organizer-v2` → `main`. Phase 2 (Whisper transcripts + semantic search) still deferred. |

## Recently Completed

- Social Media Analytics — all 4 platforms live (YouTube, TikTok, Instagram, Facebook)
- Invoice System — receipt scanner fully fixed, expenses + dates auto-logged, mileage columns added
- Payday Checklist — built, live on GitHub Pages. Auto-expense sync via Gmail + Claude Haiku + GitHub Actions. Budget countdown UI shows remaining per category. **Updated 2026-04-15:** expanded Gmail query (25+ vendor domains, Venmo/Zelle/PayPal P2P), rent payee exclusions (EXCLUDED_VENDORS in config.py), category overrides for known contacts (CATEGORY_OVERRIDES). Bank statement CSV integration designed but deferred. **Updated 2026-04-21:** UI pass — Emergency Fund goal now editable (dashed-underline input), new Engagement Ring tracker ($10K goal, $300/mo pace), removed redundant "Set aside expense budget" checkoff step, simplified "Transfer to emergency fund" step to show only the auto-synced editable goal, fixed budget-label alignment, legacy history entries auto-migrate on load.

## Ongoing / Always Active

- Email Agent — runs hourly 7am–8pm (LIVE on Mac + VPS — duplicate-run question pending resolution; Approach A iteration in progress, design at docs/superpowers/specs/2026-04-14-email-agent-fix-the-brain-design.md)
- Invoice System — CLI finance tool (LIVE on Mac)
- Dashboard — `dashboard.html` at graydavis33.github.io/my-project/dashboard.html
- **Tool Iteration Initiative** — iterating on all 14 tools one at a time, applying the AI-workflow-video framework. Log at `docs/tool-iteration-log.md`. Currently: Email Agent (iteration #1, mid-flight).
