# Decision Log

Append-only. When a meaningful decision is made, log it here.
Never delete entries — add new ones at the bottom.

Format: `[YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...`

---

[2026-03-29] DECISION: Monetize Social Media Analytics as a SaaS first | REASONING: Highest scoring project for sellability, already live on all 4 platforms, clear multi-user demand | CONTEXT: Chose SaaS model over one-time purchase for recurring revenue

[2026-03-29] DECISION: Build master framework combining Nate Herk's WAT + Liam Ottley's workspace template | REASONING: WAT gives workflows/tools discipline, Liam gives context structure + slash commands + skills system | CONTEXT: Pulling best parts of both rather than adopting either wholesale

[2026-03-29] DECISION: Skip Trigger.dev for now | REASONING: Requires TypeScript rewrite of all Python scripts — medium effort with no immediate payoff | CONTEXT: Python + Windows Task Scheduler works fine; revisit when scaling automation infrastructure

[2026-04-28] DECISION: Footage Organizer `--source` flag now defaults to MOVE (was forced COPY) | REASONING: Gray's working pattern is to dump loose footage into 02_ORGANIZED/<date>/ and let the script categorize in place — copying creates 2x disk use on exFAT D: drive | CONTEXT: `--copy` still available as escape hatch when working with footage you want to preserve at the source. Behavior change documented in workflow doc + README.

[2026-04-28] DECISION: Format detection (long-form vs short-form) is no longer purely orientation-based | REASONING: Sai now films horizontal shorts (1080p landscape for cross-posting) — orientation no longer signals editorial format. Added `--format <long-form\|short-form>` CLI override. Folder-name-based detection (vlog/long keywords) deferred to a real refactor. | CONTEXT: For now, Gray passes `--format short-form` explicitly when categorizing horizontal-but-short content. Long-term, format may live only as index metadata, not as a folder hierarchy.

[2026-04-28] DECISION: Indexer (`cli_index.py`) prunes `Adobe Premiere Pro *` and `*.PRV` subdirs | REASONING: Premiere generates `.mp4` preview renders inside its project folders — caused 585 fake "clips" in the index when Gray left a Premiere project inside `02_ORGANIZED/2026-04-27/`. Walker now skips those names defensively. | CONTEXT: Also pruned 585 stale rows from existing SQLite. Independent of where Gray stores Premiere projects — the indexer is now safe even if a project gets dropped in ORGANIZED by mistake.

[2026-04-19] DECISION: Footage Organizer detects format by orientation only (horizontal=long-form, vertical=short-form), not by resolution | REASONING: Sai workflow switched long-form from 4K to 1080p, so 4K-vs-1080p is no longer a meaningful signal; orientation is the only file-level property that maps to intended use | CONTEXT: Old code used `width >= 3840 and height >= 2160` for long-form, which would have dumped every new 1080p shoot into a `FORMAT_OTHER/` bucket

[2026-04-19] DECISION: Footage Organizer uses 17 mutually-exclusive categories with a strict "return miscellaneous when uncertain" rule — model never invents folders | REASONING: A wrong confident classification is worse than a `miscellaneous` flag because it scatters files Gray then has to hunt for; manual review of one bucket is cheap | CONTEXT: Old 13-category set had heavy overlap (broll-environment vs establishing-shots vs location-shots) which made any classifier choice look arbitrary

[2026-04-19] DECISION: Built `eval.py` harness against a hand-labeled CSV instead of tweaking the prompt by vibes | REASONING: Without a fixed test set, every prompt edit is unmeasurable — you can't tell if v2 is actually better than v1 or just differently wrong | CONTEXT: Eval bypasses cache so prompt changes actually re-run; logs go to `eval_runs/` so accuracy can be tracked across iterations

[2026-04-19] DECISION: B-roll library archive subfolders by Monday-of-week, not by exact shoot date | REASONING: Library is meant for "any interview-solo Sai's ever shot" lookup — daily granularity creates folder clutter, weekly gives a useful "roughly when" handle without splitting a single shoot's clips across multiple folders | CONTEXT: `06_BROLL_LIBRARY/{category}/{YYYY-MM-DD}/` where the date is always Monday of the ISO week containing the original shoot date

[2026-03-29] DECISION: Use Adobe Creative Cloud (not just Premiere Pro) as primary editing stack | REASONING: Gray uses full CC suite, not just Premiere | CONTEXT: Updated in context/work.md

[2026-03-29] DECISION: Skills directory stays empty until recurring workflows reveal what needs one | REASONING: Don't pre-build skills speculatively — let real usage patterns drive it | CONTEXT: Following Liam's "build organically" principle

[2026-04-09] DECISION: Keep default Chart.js bar style for Analytical dashboard charts, not isometric 3D | REASONING: Gray tested both — preferred the clean default with glow + thicker bars over the 3D block style | CONTEXT: Added custom barGlow plugin (shadowBlur: 18) and barPercentage: 0.88 to enhance the default look

[2026-04-13] DECISION: Migrate Content Researcher transcript.py to youtube-transcript-api v1.2.4 API | REASONING: Library upgraded and removed `YouTubeTranscriptApi.get_transcript()` classmethod — replaced with `YouTubeTranscriptApi().fetch()` returning FetchedTranscriptSnippet objects (attr access, not dict) | CONTEXT: All 10 transcript fetches were silently returning empty strings because the try/except swallowed the AttributeError — masked the breakage. Worth auditing other tools using the same library.

[2026-04-20] DECISION: Claude Design (released 2026-04-17) is for mockups/thumbnails/decks only — never for editing existing apps like Analytical | REASONING: Claude Design is a greenfield mockup tool that exports standalone HTML/PDF/PPTX — it does not read the repo or respect house-style code (barGlow, --accent-rgb, applyGlassVariant, analytical-feature-builder conventions). Pasting its output into real files breaks the system. | CONTEXT: Pair Design with Claude Code — Design generates visual concepts, Code implements them respecting existing conventions. Strongest day-to-day use: YouTube thumbnails (paired with Hook Optimizer), Sai pitch decks, Analytical marketing page variants.

[2026-04-20] DECISION: Hold off on Remotion adoption — revisit after more research | REASONING: Gray wants to understand the framework before committing. Potential use case (rough-cut assembler for iPhone takes) is largely already covered by python-scripts/content-pipeline/ — Remotion only earns its keep if we later want title cards/logos/intros. | CONTEXT: If pursued later, start with the official `@remotion/mcp` (docs indexer) before the community render servers (dev-arctik/remotion-video-mcp, stephengpope/remotion-media-mcp).

[2026-04-13] DECISION: Bump Content Researcher agent.py max_tokens from 6000 → 16000 | REASONING: Final 10-section report has a 450-750 word full script draft (~3k tokens alone) plus per-iteration reasoning — 6000 was causing mid-stream truncation and `max_tokens` stop_reason failures at iteration 3-5 | CONTEXT: Claude Sonnet 4.6 supports 64k output tokens; 16k gives comfortable headroom without cost bloat

[2026-04-09] DECISION: Custom metric cards persist via localStorage, not backend | REASONING: Dashboard is still a preview/static file — no backend wired yet | CONTEXT: saveCustomMetrics() / loadCustomMetrics() write to localStorage key 'analytical_custom_metrics'; will migrate to DB when backend is live

[2026-04-10] DECISION: Switch Instagram + Facebook data source from Playwright scraper to Meta Graph API (meta_fetcher.py) | REASONING: Scraper returns broken data — views default to likes count, comments/shares always 0, engagement rate shows 100% | CONTEXT: meta_fetcher.py already built and ready; needs META_ACCESS_TOKEN, INSTAGRAM_BUSINESS_ACCOUNT_ID, FACEBOOK_PAGE_ID in .env; Gray has Creator/Business account so Graph API is fully accessible

[2026-04-10] DECISION: Created Meta Developer account and app for Analytical SaaS | REASONING: Need official API access to replace broken Instagram scraper and to support future multi-user OAuth | CONTEXT: App created with use cases: "Manage messaging & content on Instagram" + "Manage everything on your Page"; currently Standard Access (Gray's accounts only); Advanced Access requires App Review when going public

[2026-04-16] DECISION: Drive/Docs file creation uses the social-media-analytics Google project, not invoice-system's | REASONING: Drive API is enabled on Google Cloud project 1097129210949 (social-media-analytics-488803) but NOT on project 528430453222 (invoice-system, email-agent, morning-briefing) — invoice-system's gspread calls hit Sheets API which is enabled, but `drive.files().create()` fails with 403 accessNotConfigured | CONTEXT: When building future tools that create Google Docs / upload files to Drive, reuse social-media-analytics/token.json (drive.file scope already granted). Token still expires every 7 days (testing mode) — use google-oauth-refresh skill.

[2026-05-01] DECISION: Footage library uses numbered weekly folders `W##_MMM-DD-DD` instead of date ranges or daily folders | REASONING: Old layout had ~600 empty stubs like `Apr 16 – Apr 19/`, `Aug 3 – Aug 9/` that sorted alphabetically (Apr/Aug/Dec) and broke within months (Aug 3 sorted after Aug 24 because string sort). Numbered weeks sort perfectly AND read as "Week N of Sai project". | CONTEXT: W01 = ISO week of 2026-04-15 (Sai project Day 1). New `create-week` CLI command runs every Monday to create that week's folder under each of the 17 categories. Library no longer pre-scaffolds future weeks — only weeks that have been "started" exist on disk. `week_utils.py` is the single source of truth for the labeling logic. Migration in `migrate_library_to_weeks.py` consolidated 16 daily folders into W01 + deleted 629 empty stubs, zero file loss.

[2026-05-01] DECISION: Hard rule — every clip exists in exactly ONE permanent location | REASONING: Storage discipline + mental clarity. Duplicates only exist temporarily in `08_QUERY_PULLS/<slug>/` during active editing, deleted via `pull-cleanup` after the edit ships. Renamed `_pulls/` → `08_QUERY_PULLS/` to make the temp-folder location visible at top level. | CONTEXT: Pulls always copy on exFAT (no hardlink support) so the rule matters most there — left alone, exFAT pull folders accumulate gigabytes of duplicate footage. `pull-cleanup` is interactive by default; `--older-than 30` for non-interactive bulk cleanup. Gray will run it manually until the cadence feels right, then automate via launchd/Task Scheduler.

[2026-05-01] DECISION: AI Edits folder promoted from `AI Edits/` (with space) to `09_AI_EDITS/` (numbered) | REASONING: Old name floated outside the numbered folder convention and had inconsistent internal structure. Promotion to `09_AI_EDITS/<source-clip>/<pipeline>/` matches the rest of the library scheme and makes outputs discoverable by source clip rather than by date. | CONTEXT: Internal reorganization deferred to a focused session — content is currently a mix of date-named (`2026-04-30/long-form/`) and source-named (`2026-04-23 Hook 1 Do Over V2/linkedin/`) folders. Both pipelines (sai-linkedin, multicam-mirror) write here.

[2026-05-01] DECISION: AI Edits internal layout chosen as pipeline-first (`08_AI_EDITS/<pipeline>/<source>/`) | REASONING: Browsing "all my linkedin posts" or "all long-form re-renders" is the more common query than "all outputs from one source clip" — pipeline-first puts the natural browsing axis at the top. Trade-off: viewing all derivatives of one source clip means checking 3 folders. Acceptable since the SQLite index can list those if needed. | CONTEXT: Migration script `/tmp/migrate_ai_edits.py` (one-off) moved 4 source folders into `linkedin/`, `long-form/`, `short-form/` subdirs. Source slugs lowercase + hyphenated.

[2026-05-01] DECISION: Eliminated `01_RAW_INCOMING/` — Gray drops loose footage directly into `01_ORGANIZED/<date>/` instead | REASONING: RAW_INCOMING was a redundant staging step. Workflow is: drop loose into ORGANIZED → run organize → clips categorize in place into `01_ORGANIZED/<category>/<date>/`. Eliminates a folder hop. All numbered folders shifted down by one (02→01, 03→02, etc.) so the scheme stays compact. | CONTEXT: Final layout is 9 numbered folders 00-08. `FOLDER_RAW` constant removed from `config.py`. Default `--source` for `run_organize()` is now `01_ORGANIZED/<date>/`. After organize completes, the empty drop folder gets deleted automatically (only if empty — won't nuke category folders by accident). 14/14 tests still pass; re-index after rename swept 263 stale paths cleanly.

[2026-05-01] DECISION: Project folders (`02_ACTIVE_PROJECTS`, `03_DELIVERED`, `04_ARCHIVE`) adopt the same `<format>/W##_MMM-DD-DD/` weekly scheme as `05_FOOTAGE_LIBRARY` | REASONING: Old project folders had ~600 empty broken-sort `Apr 16 – Apr 19` style stub folders that sorted alphabetically (Apr/Aug/Dec) — same problem the footage library had. Now all four W##-bucketed folders share one labeling scheme tied to film/publish date. Sidecar files (Adobe Premiere `.prin`, `_# Masks/` dirs) follow their parent `.prproj` by stem match. | CONTEXT: One-time migration `migrate_projects_to_weeks.py` moved 28 items, deleted 437 (Adobe regenerable cache + macOS junk + empty broken-sort stubs). All currently-loose ACTIVE projects moved to ARCHIVE because Gray confirmed they're all already published. Capitalized legacy folders (`Longform/`, `Shortform/`, `Paid Ads/`, `Onboarding/`) left alone — they pre-date the weekly scheme. `cmd_create_week` now scaffolds 26 folders per week (17 categories + 3 formats × 3 top-level project folders). Adobe cache subdirs (`Audio Previews/`, `Auto-Save/`, `Video Previews/`) are deleted on every restructure since they're regenerable on next Premiere open.

[2026-04-15] DECISION: Drop `fetch` and `puppeteer` MCP servers from `~/.claude/settings.json` | REASONING: `@modelcontextprotocol/server-fetch` never existed as an npm package (it's a Python-only package — config was broken since day one); `@modelcontextprotocol/server-puppeteer` is deprecated and unused since Python Playwright is used for all browser automation instead | CONTEXT: MCP audit found 6 of 8 configured servers were silently failing; dropping these two reduces config to 6 functional servers (filesystem, github, gdrive, obsidian, slack, sequential-thinking). WebFetch built-in tool replaces fetch MCP.

[2026-04-15] DECISION: Wrap all Windows MCP server commands with `cmd /c npx ...` instead of bare `npx` | REASONING: Node's `spawn` on Windows doesn't auto-resolve `npx` → `npx.cmd`, causing most MCP servers to silently fail at startup — `/mcp` showed only 2 connected out of 8 configured | CONTEXT: Applied to all 6 retained servers; pattern saved to memory as `feedback_windows_mcp_cmd_wrapper.md` for reuse when adding future MCP servers.

[2026-04-10] DECISION: Color theme picker in Analytical dashboard now affects the entire UI, not just the sidebar | REASONING: Gray requested full-dashboard theming; hardcoded rgba values replaced with CSS variable --accent-rgb; applyGlassVariant() now updates all CSS vars + re-renders charts | CONTEXT: preview-real.html + frontend/style.css both updated

[2026-04-11] DECISION: Use "API setup with Facebook login" (not Instagram login) for Instagram insights access | REASONING: Instagram login flow only covers messaging/content permissions — insights require the Facebook login flow which unlocks instagram_manage_insights | CONTEXT: Discovered during Meta Developer app configuration; permissions added: instagram_basic, instagram_manage_insights, pages_read_engagement, pages_show_list

[2026-04-12] DECISION: Set up Obsidian vault at C:/Users/Gray Davis/Documents/Obsidian/Graydient Media with content-niche folder structure | REASONING: Need a persistent knowledge base for session notes, scripts, and ideas that Claude can read/write via MCP across sessions | CONTEXT: Installed Obsidian via winget, added mcp-obsidian to settings.json; vault structured around video niches (Videography Tips, Editing Tips, AI & Claude Code, Camera & Gear, Journey, Effects & Showcases) + Sessions, Scripts, Ideas, Research, Business, Resources; Gray still needs to create Obsidian account and set up Sync between Windows and Mac

[2026-04-12] DECISION: Add instagram_manage_comments permission to Graydient Analytics Meta app | REASONING: Gray wants users to be able to respond to Instagram comments directly from the Analytical SaaS dashboard | CONTEXT: Permission added in Graph API Explorer alongside the 4 analytics permissions; final permission list: pages_show_list, pages_read_engagement, instagram_basic, instagram_manage_insights, instagram_manage_comments

[2026-04-20] DECISION: Rename 06_BROLL_LIBRARY → 06_FOOTAGE_LIBRARY with unused/ and used/ subfolders | REASONING: All footage — whether used in a published video or not — belongs in the library for future reuse; the used/unused split lets Gray quickly see what's available vs already deployed | CONTEXT: Archive command now sends to unused/{category}/{shoot-date}/; --mark-used DATE moves clips to used/ after publishing

[2026-04-20] DECISION: Auto-delete RAW_INCOMING folder after a successful organize run | REASONING: RAW is a temporary card dump — once clips are in ORGANIZED/ and backed up, the RAW folder is just a duplicate wasting disk space | CONTEXT: Only deletes if there are zero non-hidden skips (._files are ignored); if real clips were skipped, RAW is preserved

[2026-04-20] DECISION: Archive subfolders use exact shoot date (YYYY-MM-DD), not Monday-of-week | REASONING: Monday-of-week bucketing prevents --mark-used from distinguishing between two shoot days in the same week | CONTEXT: Overrides 2026-04-19 decision about Monday-of-week bucketing; exact date is more precise and enables per-shoot mark-used workflow

[2026-04-20] DECISION: 04_DELIVERED organized by format first (shorts/linkedin/episodes), then date | REASONING: When searching delivered content you think "which short was that?" not "what week did I publish?" — format-first matches the mental model | CONTEXT: Replaces flat date-based DELIVERED structure; mirrors how PROJECTS/ is already organized

[2026-04-20] DECISION: Rename category miscellaneous → misc | REASONING: Shorter folder name, easier to type, less clinical — same function | CONTEXT: Updated in config.py, analyzer.py prompt, and CLAUDE.md

[2026-04-12] DECISION: Instagram (@graydient_media) must be linked to Graydient Media Facebook Page before Graph API can return instagram_business_account ID | REASONING: Query 622940830905493?fields=instagram_business_account returned empty — Instagram is not yet connected to the FB page | CONTEXT: Fix: go to facebook.com/GraydientMedia → Settings → Instagram → Connect account

[2026-04-13] DECISION: Photo Organizer blur threshold lowered from 80 to 15, keep % raised from 20% to 25%, organized folder flattened (no location subfolders) | REASONING: Threshold of 80 was too aggressive for CR3 embedded JPEG thumbnails — marked ~1,774 of ~1,864 photos as blurry; flat output folder requested by Gray for easier browsing | CONTEXT: One-time script to sort ~1,800 Canon RAW photos from Mac onto Windows due to Mac storage constraints

[2026-04-13] DECISION: Create context/sai.md as a dedicated context file for the Sai Karra job | REASONING: Sai is Gray's primary income source and the role starts April 15 — keeping all job context (deliverables, expectations, Trendify background, workflow) in a dedicated file ensures it loads every session and informs any tools built for that work | CONTEXT: Offer letter + 7-day sprint doc shared; sai.md to be built once all documents are received

[2026-04-13] DECISION: Add Date column next to every Amount column in Business Expenses tab, plus mileage log section (Date, From, Destination, Miles) on the right side of the same tab | REASONING: Gray needs to know when each expense/purchase was made; mileage is tracked for shoot reimbursement purposes | CONTEXT: Business Expenses tab now has stride of 4 per category (Category | Amount | Date | spacer); total col = len(CATEGORIES) * 4; mileage section appended after Total

[2026-04-13] DECISION: Invoice System receipt scanner now falls back to HTML body extraction when no text/plain part exists | REASONING: Every modern receipt email (Anthropic, Wispr Flow, Adobe, etc.) is HTML-only — scanner was returning empty bodies and Claude correctly returned null for 100% of emails | CONTEXT: _extract_body in gmail_client.py now walks multipart tree, strips HTML tags as fallback; also added _strip_code_block to handle Claude's markdown-fenced JSON responses

[2026-04-13] DECISION: Replace dashboard usage stat boxes with simple status badge + notes system | REASONING: Auto-tracked data was inaccurate — VPS scripts never log locally so counts were always wrong; fake numbers worse than no numbers | CONTEXT: Created project-status.json (manually maintained by Claude each session); dashboard now shows LIVE/PARTIAL/RETIRED/NOT ACTIVE badge + one-line note; real run counts/costs still shown where auto-tracking is accurate (invoice-system)

[2026-04-13] DECISION: Payday checklist expense tracker shows remaining budget as countdown (not static budget amount) | REASONING: Gray wants the right-side number to decrease as spending comes in — makes it immediately obvious how much is left rather than requiring mental subtraction | CONTEXT: budget-right-input replaced with budget-remaining display + tiny editable budget sub-label; auto-fill from expenses.json overwrites all inputs on page load; GitHub Actions runs daily at 7am ET to re-scan Gmail and commit updated expenses.json

[2026-04-13] DECISION: Expense tracker uses 9 categories instead of original 5 | REASONING: Gray's real spending doesn't map cleanly to 5 broad buckets — splitting out Software & Tools, Transport, Health & Wellness, Shopping gives more actionable insight | CONTEXT: Categories: Groceries, Dining Out, Software & Tools, Streaming, Utilities, Transport, Health & Wellness, Shopping, Misc; Claude Haiku system prompt updated with explicit vendor examples per category

[2026-04-13] DECISION: Install Superpowers plugin globally (user scope) via Claude Code marketplace | REASONING: Adds 14 behavioral skills (brainstorming, TDD, systematic debugging, verification-before-completion, etc.) that enforce discipline on complex multi-file projects; Gray prefers the structure for real projects but not for small tweaks | CONTEXT: Installed via `/plugin marketplace add obra/superpowers-marketplace` + `/plugin install superpowers@superpowers-marketplace` in Claude Code CLI v2.1.105; required updating from 2.1.62 and enabling PowerShell script execution (RemoteSigned scope); Gray will tell Claude to skip the workflow on simple tasks — preference saved to memory

[2026-04-13] DECISION: Meta Graph API path (meta_fetcher.py) officially abandoned in favor of Playwright scraper (meta_scraper.py) for Instagram + Facebook data | REASONING: Playwright scraper is working in production (17 IG posts scraped, FB posts scraped) — no app verification, no Standard/Advanced Access friction, no 60-day token rotation, no OAuth flow. Keeps meta_fetcher.py in repo as reference only. | CONTEXT: Reverses 2026-04-10 decision to switch to Graph API. Discovered during skill audit — memory said "IG/FB broken, meta_fetcher is the fix" but social-media-analytics.md said scrapers are LIVE. Reality won. Updated MEMORY.md and added project_social_analytics_meta_shift.md memory file. Don't suggest Graph API as fix for IG/FB issues going forward.

[2026-04-13] DECISION: Skip full eval loop for personal workflow skills; draft-only approach acceptable | REASONING: The skill-creator's full draft→test→iterate loop (~2.5 hrs per 3 skills) is designed for general-purpose skills shipped to many users; for personal workflow skills that codify already-documented project flows, drafting from existing docs + memory is sufficient. Real use will surface triggering issues faster than synthetic evals. | CONTEXT: Built 3 skills (google-oauth-refresh, invoice-expense-logger, analytical-feature-builder) in ~15 min using this approach. Gray accepted the tradeoff. First real invocations will be the validation layer.

[2026-04-13] DECISION: Swap planned "meta-api-token-refresh" skill for "google-oauth-refresh" skill | REASONING: meta-api-token-refresh would have codified a dead-end workflow (see the Meta API abandonment decision above). google-oauth-refresh addresses the actual recurring pain — token.json expires every 7 days in OAuth testing mode, affecting both invoice-system (Gmail + Sheets) and social-media-analytics (YouTube + Sheets). | CONTEXT: Decision made mid-session when the staleness was caught. Skills pushed as commit 1503fbd.

[2026-04-14] DECISION: Delegate planning-workflow choice (/create-plan vs superpowers:writing-plans vs superpowers:brainstorming) to Claude based on task scope | REASONING: Gray doesn't want to think about which workflow fits each task; Claude has more info on each planning tool's strengths and can match to task scope (1-3 files → /create-plan, multi-file/multi-project/touches-cloud → superpowers writing-plans, fuzzy goal → brainstorming first, trivial → no plan) | CONTEXT: Heuristics baked into root CLAUDE.md as a "Planning Workflow Selection" section; Claude announces the choice when starting work; Gray can override.

[2026-04-14] DECISION: Use native Write tool to save session notes to Obsidian, NOT the obsidian MCP server | REASONING: mcp-obsidian npm package consistently failed to load (ToolSearch returned no obsidian tools despite the server being configured in settings.json); Write tool writes to any filesystem path and achieves same outcome with fewer moving parts | CONTEXT: /save command rewritten at .claude/commands/save.md to use Write tool + corrected paths (was pointing at nonexistent ~/Documents/obsidian-vault/builds/; fixed to ~/Documents/Obsidian/Graydient Media/Sessions/); broken python-scripts/obsidian_session_log.py deleted (Mac-only paths, never wired to hooks anyway)

[2026-04-14] DECISION: Keep analytical.db tracked in git for now | REASONING: Cross-machine sync convenience (Windows + Mac both pull the same DB) currently outweighs concerns; revisit when Analytical has real beta users because then (a) git history bloats with binary diffs per user signup, (b) PII risk on public GitHub repo, (c) merge conflicts on binary files become unrecoverable | CONTEXT: Discussed during CLAUDE.md audit Phase 1; alternatives for the future: Dropbox/iCloud sync + .gitignore, or migrate to hosted PostgreSQL on Railway/Supabase. Trigger condition: first real beta user signup.

[2026-04-14] DECISION: Rename json/ folder to references/ rather than deleting | REASONING: Folder contains Nate Herk's WAT framework + Liam Ottley's workspace template + Trigger.dev SDK reference — source materials Gray used to build the current workspace; deleting would lose the "what I learned from" library | CONTEXT: Ghost CLAUDE.md files inside (auto-loaded by Claude when cd'd into the folder) were the real problem — renamed both to wat-framework-reference.md and workspace-template-reference.md so they're inert but preserved. Folder now lives at references/ with updated CATALOG.md. Set a 6-month calendar check: if the folder is untouched by October, reconsider deletion.

[2026-04-14] DECISION: Phase 4b (dashboard card coverage) deferred indefinitely | REASONING: project-status.json has 21 projects but dashboard.html has only 7 rich cards; adding 13 more requires either ~3 hours of hand-crafted content (each card has priority/completion%/what-it-does/what's-left/score-pills) or a simpler "compact card" component that doesn't exist yet — not worth the investment without a specific driver | CONTEXT: CLAUDE.md audit Phase 1-4a complete; Phase 4b marked complete-as-skipped. Revisit if Gray needs the dashboard to show his full portfolio (e.g., for a pitch or public page).

[2026-04-14] DECISION: Future Footage Organizer v2 concept — hand-symbol tagging on the last frame of each clip — captured but gated on current-version testing | REASONING: Idea is stronger than current 4-frame content analysis (cheaper at 1 Vision call vs 4, more accurate because intent > inference, backwards compatible because no-symbol clips fall back to current behavior) — but untested on real footage; adding complexity before proving the baseline is premature | CONTEXT: Idea stored in python-scripts/footage-organizer/FUTURE_IDEAS.md (full design), Obsidian Business/Projects/Footage Organizer.md (short blurb), and Claude persistent memory (project_footage_organizer_hand_symbols.md). Gray saw the technique from another videographer; symbols would mean good/bad/player/trick/duration, stored as metadata tags (JSON sidecar) not folder assignments.

[2026-04-14] DECISION: Content Pipeline transcription uses local Whisper (not OpenAI API), default model large-v3, no timestamps in output | REASONING: Local Whisper is free, no 25MB file limit, runs offline, same accuracy as API; large-v3 is the most accurate model and Gray prioritized accuracy over speed; timestamps bloated the file and Gray only wanted prose for planning | CONTEXT: Added --transcribe-only flag to main.py; CPU-only transcription is ~real-time (53s base / ~15min large-v3 for 16-min audio); RTX 5070 CUDA upgrade deferred. yt-dlp installed for downloading YouTube audio — pattern: yt-dlp -x --audio-format mp3 -o "input/NAME.mp3" URL then python main.py input/NAME.mp3 --transcribe-only.

[2026-04-14] DECISION: Start Tool Iteration Initiative — apply the AI-workflow-video framework to all 14 existing tools, one at a time, highest-impact first | REASONING: Gray transcribed a 16-min video outlining a framework (educate AI → master prompt → Role-Context-Command-Format → SOPs → skills → chained agents) and wants to iterate on existing tools rather than build new ones; doing all 14 simultaneously would drown him; going one-by-one makes each pass a real review | CONTEXT: Three approach types defined — A (fix the brain: context + prompts), B (feedback loop: capture rejections), C (skill-file refactor). Running log at docs/tool-iteration-log.md; specs at docs/superpowers/specs/. Email Agent is iteration #1.

[2026-04-14] DECISION: Email Agent iteration #1 uses Approach A only (not a rewrite) — build shared role_context.md master prompt, rewrite classifier + drafter prompts using Role-Context-Command-Format formula, kill stale "social media marketer" line, add --dry-run flag | REASONING: Smallest high-ROI upgrade that produces a reusable master-prompt artifact for later tool upgrades; no architectural changes, easy rollback via git revert | CONTEXT: Design doc at docs/superpowers/specs/2026-04-14-email-agent-fix-the-brain-design.md. Session paused before implementation plan — resume point in tool-iteration-log.md.

[2026-04-14] DECISION: Tool Iteration tracker lives as a standalone page at web-apps/tool-iteration/ — NOT a tab inside dashboard.html | REASONING: Gray explicitly wanted iteration tracking visually separated from project-status tracking because the iteration initiative is about "remodeling" projects, not tracking their day-to-day status; standalone gives more canvas to lean into futuristic/AI-like aesthetic (glass panels, glow, monospace stats, ambient gradient bg) without fighting the existing dashboard design | CONTEXT: Data-driven from iteration-data.json — single file to update when a tool advances (status/approach/steps). Works both locally (python -m http.server) and on GitHub Pages. No framework, pure HTML/CSS/JS. Live URL: graydavis33.github.io/my-project/web-apps/tool-iteration/index.html.

[2026-04-14] DECISION: Sai long-form visual/editing style reference = Liam Ottley Vlogs | REASONING: Gray admires the channel's clean documentary-cinematic aesthetic — authentic/real but cinematic, light color grade, voiceover over B-roll, chill ambient music, location-driven. Positional parallel is exact: Liam = 23yo AI agency CEO (Morningside AI), Sai = 21yo UGC agency COO (Trendify). Both have split channel strategy (education + vlog/BTS). Style matches the "Raw" framing that drove the 12x view-to-sub outlier in Gray's own founder-vlog research. | CONTEXT: Spec in plans/2026-04-15-sai-debrief-content-strategy.md Section 2. Action: Gray watches 3 most-viewed Liam Ottley Vlogs episodes, screenshots 10–15 reference frames per shot category before first shoot.

[2026-04-14] DECISION: Sai Episode 1 reframed from generic "young founder intro" to "How I Built My 2nd Agency Before 22" (BuiltGen → Trendify arc) | REASONING: Deep research revealed Sai isn't a first-time founder. BuiltGen (his prior agency with Srikar) generated 20M+ leads for 50+ clients. Trendify is his 2nd, professionalized, doing $722K ARR (2x YoY) with YC/Mindvalley/Justin Kan clients. "2nd agency" is a far stronger narrative hook than "meet me." Cuts through the saturation of "introducing myself" founder videos. | CONTEXT: Strategy doc Section 2 + research dossier section 11. Changes title candidates, cold-open line, and the "how I got here" act of the episode.

[2026-04-14] DECISION: Seriously consider featuring Srikar (Sai's older brother, Trendify CEO) as co-host — propose "The Karra Brothers" as series title Option F | REASONING: Combined brothers = 15M+ followers (Sai 5M + Srikar 10M across brand pages). Vlogbrothers precedent (John + Hank Green → Complexly media empire) proves sibling content duos scale into decade-plus media assets. Sibling dynamic is the #1 differentiator vs every other solo-founder vlog in the space (Liam Ottley, Iman Gadzhi, etc are all solo). Different complementary public voices — Srikar is teacher/chronicler ("How I Scaled 10M TikTok Followers Organically"), Sai is storyteller. | CONTEXT: Gated on Sai's willingness to bring Srikar on camera — question #3 in tomorrow's debrief. New Format 8 "Brother Banter" and Format 9 "Lessons from BuiltGen" added to short-form rotation.

[2026-04-14] DECISION: Sai YouTube channel growth benchmarks updated to 5–10x zero-start baseline | REASONING: Sai has 5M+ followers across platforms already (started YouTube 2015, TikTok hit 1M in 3 months of daily posting Jan 2021). Generic "zero-start" benchmarks (500–1,000 subs in 90 days) don't apply. Realistic targets: 5K–20K subs in 30 days, 30K–50K in 90 days, 100K in 6 months if cross-pollination from his + Srikar's existing audiences works modestly. Stretch: 100K in 90 days, 500K+ in 12 months. | CONTEXT: Strategy doc Section 6. Pitching these to Sai tomorrow — he'll either validate or provide his own targets.

[2026-04-14] DECISION: Sai footage organization = 8 numbered top-level folders, exFAT SSD cross-machine, Footage Organizer integrated at step 2 of daily workflow | REASONING: Gray needs a filing system that works on Windows AND Mac (his setup), survives 5h/day shoot cadence, keeps raw separate from edits separate from delivered separate from reusable B-roll, and plays nicely with Footage Organizer Python tool. Numbered folders (00_TEMPLATES → 07_ASSETS) force predictable sort order. Daily pipeline: RAW_INCOMING → Footage Organizer → ORGANIZED (content subfolders) → PROJECTS/shorts/DayN → DELIVERED + _DELIVERY_LOG. | CONTEXT: Full spec at plans/2026-04-15-sai-footage-organization-system.md including naming conventions (YYYY-MM-DD, Day001, E01, TT/IG/YT/LI codes), daily + weekly workflows, backup strategy (3-2-1), and a bash script that creates the entire folder tree with one command.

[2026-04-14] DECISION: Strategy doc distribution uses GitHub mobile view, NOT Google Docs | REASONING: Gray asked to put plans/2026-04-15-sai-debrief-content-strategy.md into a Google Doc. Investigated — gdrive MCP server configured only exposes read tools, no document create/share capability from Claude Code. Options were: (A) GitHub mobile URL (zero setup, tables render, headers collapse, wikilinks clickable), (B) manual paste into blank Google Doc, (C) build a Python script to convert .md → Google Doc via existing OAuth (~20 min). Chose A for tonight because faster and GitHub's mobile rendering is actually better than Google Docs for this doc type. | CONTEXT: Reusable MD→GDoc Python script is a valid "weekend project" for later if Gray wants it for ongoing strategy docs, research reports, etc. Would live at python-scripts/shared/markdown_to_gdoc.py using invoice-system's existing token.json (already has drive.file scope).

[2026-04-15] DECISION: Added Slack MCP and Sequential Thinking MCP to ~/.claude/settings.json; keeping Slack bot token + team ID in plaintext in settings.json env block | REASONING: Slack MCP lets Claude list channels, post messages, and read threads directly (Gray commands agents through Slack). Sequential Thinking MCP gives structured step-by-step reasoning for iteration/planning work (the "iterations" use case Gray flagged last session). Tokens in ~/.claude/settings.json is the standard MCP configuration pattern — that file is user-global, not in the repo, never pushed to GitHub. The repo-level gitignore rules for .env/token.json don't apply to user-global config. Real risks are cloud-sync of user folder (check OneDrive) and screen-sharing settings.json — neither is happening. | CONTEXT: Both servers require restart of Claude Code to load. Slack MCP = archived upstream (still functional, community fork @zencoderai/slack-mcp-server is the maintained replacement if bugs surface). Feedback memory saved at ~/.claude/projects/.../memory/feedback_mcp_tokens_in_settings.md so I don't false-alarm on this next session.

[2026-04-15] DECISION: MCP config belongs in ~/.claude.json, not ~/.claude/settings.json — merged 5 missing servers into ~/.claude.json at user scope and cleaned up orphaned case-duplicate project entry | REASONING: Gray reported only 2 MCP servers (filesystem + magic) connecting despite 6 configured in ~/.claude/settings.json. Traced the Claude VS Code extension logs — it spawns the CLI with --strict-mcp-config, which tells Claude to read ONLY from the passed-in config and ignore ~/.claude/settings.json entirely. The actual MCP config used lives in ~/.claude.json (file, not directory). Also discovered two project entries under different cwd casing (c:/... vs C:/...) — only the one matching current session casing is active, the other orphans. Previous 2026-04-15 decision about adding Slack + Sequential Thinking MCPs to settings.json was incorrect — they never loaded. | CONTEXT: Backup of original .claude.json at ~/.claude.json.bak-20260415-160208. Filesystem server now includes both my-project and G:/ paths. User-scope servers after merge: magic, github, gdrive, obsidian, slack, sequential-thinking. Needs VS Code restart for the 5 new servers to connect. Reference memory saved at memory/reference_mcp_config_location.md so future sessions don't repeat the mistake of editing settings.json for MCP.

[2026-04-15] DECISION: Expense tracker now excludes rent-related Venmo payees via EXCLUDED_VENDORS list in config.py + overrides P2P categories via CATEGORY_OVERRIDES map | REASONING: Venmo P2P payments ("You paid X $Y") were either skipped entirely by Haiku (prompt said "not a personal purchase receipt") or classified as Misc. Large rent payments (A K $1175, Jodi Ammons $450, Ohana Housing) inflated the expense budget; small P2P (Garrett ODell $40) needed correct category (Dining Out, not Misc). Post-filter in main.py is more reliable than relying on Haiku to exclude/categorize correctly. | CONTEXT: EXCLUDED_VENDORS: ["A K", "Jodi Ammons", "Ohana Housing"]. CATEGORY_OVERRIDES: {"Garrett ODell": "Dining Out"}. Gray can add more payees as they come up. Gmail query also expanded from ~15 vendor domains to ~40+ (Venmo, Uber, CVS, Target, gym chains, etc.). April expense coverage went from 3 items ($85) to 8 items ($176).

[2026-04-15] DECISION: Bank statement CSV integration for payday checklist deferred — architecture designed but not built | REASONING: Gray wants it but no CSVs currently available; designed a safe architecture (CSVs stored outside repo in ~/Documents/bank-statements/, never committed, pre-commit hook blocks CSV commits, only derived expenses.json goes to git, Haiku categorizes vendor descriptions, dedup against Gmail entries by date+amount). Estimated ~$0.05/month Haiku cost. | CONTEXT: Gray will revisit when ready to export a CSV from his bank. Needs: which bank (Chase/Amex/etc), BitLocker confirmation, folder path confirmation.

[2026-04-16] DECISION: Footage Organizer rebuilt with numbered folder structure (00_TEMPLATES → 07_ASSETS) matching the Sai footage organization plan | REASONING: Previous build was lost when Gray reconnected the my-project workspace folder without saving — git pulled older version and overwrote local changes. Rebuild matched the plan doc at plans/2026-04-15-sai-footage-organization-system.md with 13 expanded content categories. 06_BROLL_LIBRARY is the global reusable footage library (no dates, by category); --archive command sends unused ORGANIZED clips there. | CONTEXT: Libraries built at /Volumes/Footage/Sai/ and /Volumes/Footage/Graydient/. .env set with correct SSD paths. ffmpeg installed via Homebrew. First real test with FX3 footage running end of session.

[2026-04-19] DECISION: Obsidian vault lives on Google Drive, not ~/Documents | REASONING: Vault syncs via Google Drive for cross-device access; actual path is ~/Library/CloudStorage/GoogleDrive-graydavis33@gmail.com/My Drive/Obsidian/Graydient Media | CONTEXT: Updated MCP path in ~/.claude.json accordingly

[2026-04-19] DECISION: Never read .env files | REASONING: Gray explicitly does not want Claude to access API keys or secrets in .env files — keys stay under Gray's control only | CONTEXT: Triggered after Claude copied API key from footage-organizer/.env without asking

[2026-04-19] DECISION: Use Google Drive for Desktop (Mirror mode) to sync Obsidian vault between Mac and Windows — not Obsidian Sync service | REASONING: Gray already has 100GB Google Drive plan; Obsidian Sync is $4/mo extra; Mirror mode keeps full vault on disk locally (required — Obsidian needs all files present for indexing/search/graph); Stream mode would break it. Windows vault was canonical (had more content) so copied C:\Users\Gray Davis\Documents\Obsidian\* → C:\Users\Gray Davis\My Drive\Obsidian\. Mac's existing Google Drive vault merged in cleanly — only collision was Sessions/2026-04-16-session.md (different content on each side), preserved both by renaming Windows version to -windows.md. Graydient Media/ is the vault root (has its own .obsidian/). Old top-level .obsidian/ from Windows preserved as .obsidian-windows-backup/ in case plugin settings needed later. | CONTEXT: Vault path is C:\Users\Gray Davis\My Drive\Obsidian\Graydient Media\ on Windows and ~/Library/CloudStorage/GoogleDrive-graydavis33@gmail.com/My Drive/Obsidian/Graydient Media/ on Mac. MCP config in ~/.claude.json updated on Windows; Mac's ~/.claude.json updated in earlier session. Gitignore tightened (added token*.bak* + *.bak.* patterns) after push protection caught a timestamped OAuth token backup in history — old *.bak alone wasn't catching timestamp-suffixed files. Content Pipeline config.py now reads OBSIDIAN_SAI_CONVERSATIONS from .env so the --meeting-notes path is cross-platform (Windows override: ~/My Drive/...; Mac override: ~/Library/CloudStorage/GoogleDrive-.../My Drive/...).

[2026-04-19] DECISION: Voice memos get a dedicated `Voice Memos/` folder in the Obsidian vault (separate from `sai-karra/Conversations/`), with `_Index.md` as the hub and per-memo backlinks | REASONING: Voice memos are a distinct content type (brainstorm/meeting captures with their own workflow), not Sai-specific, and an index + backlinks turn the folder into a connected knowledge graph rather than a flat dump | CONTEXT: Renamed script env var `OBSIDIAN_SAI_CONVERSATIONS` → `OBSIDIAN_VOICE_MEMOS`; updated config.py, main.py, README.md, .env; auto-backlink insertion added to `save_meeting_notes()`

[2026-04-19] DECISION: Date source for voice memos = manual `YYYY-MM-DD - ` filename prefix, NOT embedded metadata | REASONING: iOS re-wraps the `.m4a` container on share-sheet export, overwriting every creation timestamp (mvhd, iTunes atoms, QuickTime creation_date). No metadata survives. Upstream capture is the only reliable path. | CONTEXT: Short-term workaround: rename files manually. Long-term fix: iOS Shortcut that stamps date at export, OR switch to Just Press Record app ($5) which names files with recording date natively.

[2026-04-19] DECISION: PyTorch CUDA 12.8 (`cu128` wheel) is the target build for Blackwell GPUs on Windows | REASONING: RTX 5070 is sm_120 Blackwell; CPU-only torch was the default install and slows Whisper ~10x vs GPU. cu128 wheel supports sm_120 and gives ~650 frames/sec on large-v3 vs ~60 fps on CPU. | CONTEXT: Uninstall: `pip uninstall torch -y`; install: `pip install torch --index-url https://download.pytorch.org/whl/cu128`. Verify with `torch.cuda.is_available()`.

[2026-04-19] DECISION: Current content production system for Sai = ONE weekly YouTube vlog (8–10 min, cinematic) + 3 daily short-form posts (minimal edits, phone, off-the-cuff); shoot 1080p not 4K | REASONING: They iterated the system 3x on 2026-04-19. The final (5 PM, New Recording 7) replaces "Production systems" (4:18 PM) which had proposed daily vlogs + batch weekend shorts — too unscripted and too much production. Narrowing to one excellent weekly piece + spontaneous shorts matches Sai's best-performing content historically. | CONTEXT: Saved to memory as project_content_system_current.md. Source of truth: Obsidian/Graydient Media/Voice Memos/2026-04-19-New Recording 7.md. Test for one full week before changing anything. Guiding principle: intentionality.

[2026-04-20] DECISION: 100 Videography Tips series — move the series identifier ("This is Part X of 100 videography tips anyone can learn") from the 0:00 opener to the CTA at 0:27–0:30 | REASONING: Frees the first 3 seconds for a sharper workflow-claim hook (e.g. "Stop taking your SD card out of your camera"). Opening with the series line commits viewers before the value is shown; closing with it converts viewers who already got value into binge-watchers for other parts. | CONTEXT: Applied first to Part 3 (SD card offload). Script structured as Hook → Why → How it helps (5 rapid bullets) → How to do it → CTA. Pattern should propagate to Parts 1, 2, and future parts.

[2026-04-20] DECISION: Keep Footage Organizer `.cache.json` committed to git as the cross-machine sync layer; add a read-only `git fetch` + `git log HEAD..@{u} -- .cache.json` guardrail in `main.py` that warns + prompts before any cache-touching command (organize, archive, mark-used) if the remote is ahead. | REASONING: The April 16 misc/ bug happened because Windows ran `--archive` before pulling the Mac's `.cache.json` update — 40 clips got no cache hit and fell back to misc/. Rejected alternatives: (a) auto-pulling (destructive, could blow away local work), (b) cache in cloud storage (adds a new failure mode), (c) manual pull discipline (what failed the first time). Git-as-sync-layer is already the existing pattern — the fix just enforces that pattern at the tool boundary. | CONTEXT: Check is non-blocking if git is unavailable or no upstream is set (fails silently). Default prompt answer is "no" — user must type `y` to override. Tested live 2026-04-20 on Windows — caught commit `cd932e6` where Mac had removed 5 stale entries, exactly the scenario it's designed for.

[2026-04-20] DECISION: Delete `ui-ux-pro-max` custom skill and `magic` MCP server | REASONING: ui-ux-pro-max had 0 logged invocations in 18 days and its trigger set was so broad (10 action verbs × unlimited project types) it would hijack project-specific skills like analytical-feature-builder; magic MCP was UI component generation which is now superseded by Claude Design. | CONTEXT: First cleanup pass from workspace audit (audits/2026-04-20-workspace-audit.html). Also fixed stale `~/Desktop/my-project` path in session-habits.md, corrected CLAUDE.md MCP table (removed stale fetch + puppeteer entries, added slack + sequential-thinking, updated config-file location from settings.json to .claude.json). Skills count goes 4 → 3, MCP count stays at 6 (magic out, but table now reflects reality).

[2026-04-21] DECISION: Podcast Publisher runs fully autonomous — no draft-review checkpoint | REASONING: Gray wants the pipeline to kick off and publish straight through; audit ex-post and iterate the SOP rather than gate every run | CONTEXT: Intro placed immediately after hook (rule-based), midway placed at Claude-picked mini-hook in middle third, RSS.com drives via Playwright through Publish

[2026-04-21] DECISION: Podcast pipeline reads from local D:/ only, no Google Drive API | REASONING: Gray manually pulls raw pods from Drive to D:/Sai/Podcasts/Incoming/ — avoids OAuth/token expiry complexity, keeps the code simple, mirrors how footage already flows | CONTEXT: Stingers are static assets at D:/Sai/07_ASSETS/Podcast Stingers/ (same two files every episode)

[2026-04-21] DECISION: Screen recording → SOP is a reusable on-ramp for delegating any workflow to AI agents | REASONING: Every task Sai (or anyone) hands off starts as a screen recording; standardizing frame-extraction + transcription + structured SOP template turns it into repeatable input for automation | CONTEXT: New tool at python-scripts/screen-recording-analyzer/, workflow at workflows/screen-recording-to-sop.md — first use case was Sai's podcast upload

[2026-04-21] DECISION: Delete photo-organizer, client-onboarding, 4 dead web-app prototypes (brand-board, invoice-builder, invoice-template, web-apps/social-media-analytics). KEEP ai-shorts-channel. | REASONING: Gray reviewed the audit cut list: photo-organizer duplicated footage-organizer (Claude Vision file sorter); client-onboarding was explicitly retired 44 days ago; the 4 web prototypes had no backend / no dashboard link / no recent edits. ai-shorts-channel is NOT dead — Gray intends to use it once accounts are set up. | CONTEXT: Executed autonomously during sleep window. 6 folders removed. Orphan-doc deletions deferred — Gray wants to read the 4 orphan docs first, and (update) screen-recording-to-sop.md turned out to NOT be an orphan (actively used for Sai's podcast workflow per today's earlier entry).

[2026-04-21] DECISION: Email agent is a long-lived daemon, not a cron-fired one-shot — VPS is the sole canonical runner | REASONING: main.py uses the Python `schedule` library internally to handle its own 7am–8pm hourly cadence + a 30s poll loop. Running it via launchd/cron/Task Scheduler "every hour" spawns stacking daemons (duplicate Slack DMs, duplicate invoice scans, voice-profile race). VPS systemd `Restart=always` is the right shape. Disable any Mac launchd or Windows Task Scheduler entry for email-agent. | CONTEXT: Full investigation at audits/2026-04-20-email-agent-duplicate-run-investigation.md. Also noted: deploy/email-agent.service (committed) and the inline service block in deploy/vps-setup.sh drift on Restart policy + log path — vps-setup.sh overrides the committed file on re-deploy.

[2026-04-21] DECISION: Codify build discipline as a persistent rule at .claude/rules/build-discipline.md | REASONING: The audit exposed 4 overlapping tool pairs (expense-tracker/invoice-system, photo-organizer/footage-organizer, creator-intel/content-researcher, screen-recording-analyzer/content-pipeline), 6 of 8 workflow docs drifted 1–3 versions behind code, and one broadly-triggered skill (ui-ux-pro-max) sat unused for weeks. These are CATEGORIES of failure, not one-off bugs — need a persistent rule loaded every session, not just a note in decisions/log.md. | CONTEXT: New rule covers: extend-existing-tools-before-building (with ≥50% overlap threshold), specific-context skill triggers (not generic verbs), one-scheduler-per-job, doc-drift-is-a-bug (same-commit updates), config-drift-is-worse, cache_control on multi-step Claude calls, CLAUDE.md as loaded-every-session map. Companion artifact: audits/2026-04-20-workspace-audit.html as the baseline for future quarterly audits.

[2026-04-21] DECISION: Refreshed 6 stale workflow docs to match current code in one pass | REASONING: Stale workflow docs are actively misleading — they describe architectures that no longer exist. Fixing them all in one pass is faster than refactoring them piecemeal, and batches the learning into one commit. | CONTEXT: content-researcher (old 5-step pipeline → agentic loop + Reddit + 10-section report), content-pipeline (added --transcribe-only, --meeting-notes, --all batch, local Whisper CUDA), footage-organizer (near-full rewrite: --client, --archive, --mark-used, 17 categories, cache-sync guardrail), invoice-system (added scan-payments, scan-all --schedule, add-expense, 9 expense categories), social-media-analytics (GitHub Actions schedule, trends, post explanations, dashboard JSON export), email-agent (daemon architecture + invoice integration + followup tracker + duplicate-run warning).

[2026-04-21] DECISION: Shipped Content OS sub-projects A (playbook edits), C (file-system half), and D (tool inventory) in one overnight build; B/E/F deferred to await real production data | REASONING: A/C/D are pure doc + scaffolding work — no paid API calls, no creative decisions, no production data dependency. Safe to run solo while Gray sleeps. B (Sai playbook) needs post-Week-1 analytics; E (Content OS command-center tool) is month-2 work; F (per-workflow skill.md builds) needs real content-pipeline runs to write accurately. Splitting safe-to-automate vs needs-Gray was the right call. | CONTEXT: See decisions/content-os-log.md for the per-sub-project detail. Output: playbook now 7 layers + 7 series; Layer 7 (Iteration & Tagging) backed by video-log.csv + killed-archive.md + winning-patterns.md; tool-inventory.md picks Content Researcher → Hook Optimizer → Content Pipeline → Creator Intel → Social Analytics → Footage Organizer as priority iteration order; 6 SKILL.md skeletons exist but are marked SKELETON and should be filled in as each tool advances stages.

[2026-04-21] DECISION: Codify the workspace-audit workflow as an SOP at workflows/workspace-audit.md, and add a shared sop-template.md for all future workflows | REASONING: The audit was valuable and will re-run quarterly — re-deriving the 5-agent dispatch pattern + verification-before-deletion discipline each time is wasteful. SOP + template is the cheapest possible reuse: no new tool, no new runtime, no maintenance surface. Explicitly rejected building a dedicated "SOP-creator" agent as over-engineering (~1-2 SOPs/month = too little cadence to justify a tool, and would repeat the overlap-sprawl pattern the audit just fixed). | CONTEXT: SOP includes self-contained agent prompts for all 5 research scopes, the pre-flight cross-reference check (born from the screen-recording-to-sop.md near-miss), branded HTML output spec, and tiered execution policy. Template at templates/sop-template.md uses the de-facto shape every workflows/*.md already shares. Both files referenced from build-discipline.md so they load every session.

[2026-04-21] DECISION: Footage Organizer — move is now default behavior (RAW folder auto-deleted after successful organize run); --copy flag added for opt-in copy mode | REASONING: Old default was copy, leaving duplicate clips in RAW_INCOMING — every organize run created two versions and required manual cleanup | CONTEXT: Replaces the --move flag; behavior change is backward-compatible for anyone not using flags

[2026-04-21] DECISION: Remove unused/used split from 06_FOOTAGE_LIBRARY — flat structure: category/date/ | REASONING: The split added friction and complexity with no real payoff for a solo operator — Gray never needed separate "I used this" tracking enough to justify two parallel folder trees | CONTEXT: Migrated all clips from unused/{category}/{date}/ → {category}/{date}/; used/ (empty) deleted; --mark-used command removed from main.py and config.py cleaned up

[2026-04-21] DECISION: Rename 03_PROJECTS → 03_ACTIVE_PROJECTS; seed 52 week folders (Apr 16 2026 – Apr 11 2027) across Footage Library, Delivered, Archive, Active Projects | REASONING: Week-based folder structure matches how Gray thinks about content output (weekly cadence with Sai); named by calendar dates (e.g. "Apr 20 – Apr 26") not ISO week numbers for readability; shot-type → week order in Footage Library because editing always starts with "what type of shot do I need" | CONTEXT: Archive gets long-form/ and short-form/ as the top level; Delivered and Active Projects keep shorts/linkedin/episodes split; old BROLL_LIBRARY (empty shells only) deleted

[2026-04-21] DECISION: Seed a new `python-scripts/asset-generator/` project for procedural visual-asset generation (first demo: confetti_burst.py → ProRes 4444 .mov with alpha for Premiere) | REASONING: None of the 14 existing tools do visual asset generation — closest neighbors are content-pipeline (video transcription/clipping) and footage-organizer (sorting existing footage), neither overlaps. Per build-discipline rule 1, new-project bar is high but "no existing tool does ≥50% of the proposed work" clearly applies. Goal is a long-term path toward a free, owned Motion Array / Epidemic Sound alternative: procedural SFX (BFXR/sfxr-style JSON recipes), Lottie motion graphics, Remotion video templates, overlay packs — all code-owned and re-generable. Today's confetti burst is the seed prototype; system design (folder structure, JSON recipe format, CLI, gallery UI) deferred to a dedicated planning session. | CONTEXT: python-scripts/asset-generator/confetti_burst.py (200-particle radial burst, gravity + drag + rotation, transparent RGBA PNG sequence → yuva444p10le ProRes 4444 MOV via ffmpeg). Output: 21MB .mov at output/confetti_burst.mov, drops straight onto Premiere timeline over any layer. All tuning knobs (count, speed, gravity, palette, duration) at top of the script.

[2026-04-21] DECISION: In the Payday Checklist, keep the 9-category per-row editable budgets intact and simply remove the redundant "Set aside expense budget" checkoff step (chose option A of A/B/C). | REASONING: The per-category budget structure is load-bearing — it's what the Gmail auto-expense-sync flow routes spend into (CATEGORY_OVERRIDES in config.py). Replacing it with a single editable lump sum (option B) would kill auto-categorization entirely and force manual categorization for every receipt. Option C (lump sum master + 9 categories) adds a conflicting source of truth with no clear precedence. Option A keeps the working auto-sync, removes only the redundant checkoff (the budget's already tracked on the right side of the UI), and makes Gray's mental model of "what's my total budget" still visible via the auto-summed total at the top of the section. | CONTEXT: Payday Checklist at web-apps/payday-checklist/index.html. STEP_NAMES went from 6 entries to 5 ("Expense Budget" removed). "Transfer to emergency fund" renumbered step-5 → step-4. Legacy history migration added in loadHistory() to shift step[5] → step[4] on first read after deploy (one-time, idempotent). Per-category budgets (budget-0..8) unchanged.

[2026-04-21] DECISION: Auto-sync the "Transfer to emergency fund" checkoff step's displayed goal ($9,000) with the editable goal input in the Emergency Fund card, rather than hardcoding it in two places. | REASONING: Making the goal editable in one spot while displaying a stale hardcoded version in the checkoff step is a classic drift-bug waiting to happen — Gray edits goal to $12K in the card, but the checkoff step still says "$9,000 Savings goal" and he trusts the wrong number. Single source of truth (the fund-goal input) driving both displays via updateFund() eliminates the drift. 3 lines of JS: `const stepEl = document.getElementById('step-fund-goal'); if (stepEl) stepEl.textContent = '$' + goal.toLocaleString();`. Same pattern should apply if we ever make the $1,500/mo savings rate editable too (currently hardcoded in updateFund's monthsLeft calc). | CONTEXT: Step-4 right-side amount now has id="step-fund-goal" and is updated on every updateFund() call. Gray explicitly asked to "only say 'transfer for emergency fund', and then it's a $9,000 goal" — wanted the step to reflect the goal, not a monthly transfer target, so this matched his mental model.

[2026-04-26] DECISION: Created a new "Karra Media Automation" Google Cloud project (ID: karra-media-automation, OAuth client 261867840059) under gray@karramedia.com — separate from graydavis33's social-media-analytics-488803 project — for all karramedia.com / Trendify-facing Drive + Docs automation. Built `python-scripts/gdocs-cli/` tool against it. | REASONING: Work output (Trendify props doc, recommendation letters for Karra-side contacts, future Karra automation) belongs on a Karra-owned Cloud project rather than mixed into the personal social-media-analytics-488803 project. Clean separation of personal vs work API quotas/billing, and the Doc lands in the karramedia.com Drive natively (auth user owns the file). Did NOT just enable Docs API on the existing social-media-analytics-488803 project (Path 1) because long-term it's the wrong home for work-output Drives. | CONTEXT: New OAuth Desktop client `client_secret.json` lives at `python-scripts/gdocs-cli/client_secret.json`; old graydavis33 client backed up as `client_secret.OLD-graydavis33.json.bak`. Token expires every 7 days (testing mode) — same `google-oauth-refresh` skill pattern applies. Scopes: `documents` + `drive.file`. Companion script `build_trendify_props.py` is a one-off that uploaded 9 Pillow-generated icons + the resized Trendify logo to Drive, created the Doc, attached the logo as a centered page footer, and built 9 pages with consistent template (2 leading empty centered lines → 255.375pt centered icon → 32pt Montserrat Semi-Bold orange title → page break). All future Karra Media tools should reuse this Cloud project rather than spin up new ones.

[2026-04-28] DECISION: Standardize the `video-use` AI editor's output structure as 3 deliverables in `D:/Sai/AI Edits/<date>/` — `final.mp4`, `edl.json` (with cut decisions + per-beat reasoning baked in as the cut log), `master.srt` (output-timeline transcript). Plus `transcripts/` + `takes_packed.md` kept around as cached intermediates. Intermediate render artifacts (`base.mp4`, `clips_graded/`) get deleted post-render. | REASONING: Gray's question — "is keeping the EDL JSON + transcript wasteful?" — surfaced that the expensive work (Scribe transcription + Claude cut reasoning) is already paid for whether you save the byproducts or not. Saving the EDL means re-edits cost zero (tweak one number → re-render in 30s), saving the transcript means future re-cuts on the same footage skip another Scribe charge. Intermediate render files are cheap local ffmpeg, regeneratable from sources + EDL, so they're disk waste and get cleaned. The folded "cuts log" lives inside `edl.json` rather than a separate file — same data. | CONTEXT: First end-to-end production run of `video-use` on Sai 2026-04-28 footage (3 clips, 3:20 source → 32s final). EDL fields used per range: `source`, `start`, `end`, `beat`, `quote`, `reason`. Top-level EDL also includes `dropped_sources` (per-clip rationale for what was excluded entirely — C2324 chatter, C2323 weaker-hook take) and `spec` (resolution/fps/padding). Padding spec also captured: 0.25s lead + 0.25s tail per beat → 0.5s natural dead space between beats. Re-use this output shape for every future video-use edit.

[2026-04-28] DECISION: Footage Organizer `--source` flag now defaults to MOVE (was forced COPY) | REASONING: Gray's working pattern is to dump loose footage into 02_ORGANIZED/<date>/ and let the script categorize in place — copying creates 2x disk use on exFAT D: drive | CONTEXT: `--copy` still available as escape hatch when working with footage you want to preserve at the source. Behavior change documented in workflow doc + README.

[2026-04-28] DECISION: Format detection (long-form vs short-form) is no longer purely orientation-based | REASONING: Sai now films horizontal shorts (1080p landscape for cross-posting) — orientation no longer signals editorial format. Added `--format <long-form|short-form>` CLI override. Folder-name-based detection (vlog/long keywords) deferred to a real refactor. | CONTEXT: For now, Gray passes `--format short-form` explicitly when categorizing horizontal-but-short content. Long-term, format may live only as index metadata, not as a folder hierarchy.

[2026-04-28] DECISION: Indexer (`cli_index.py`) prunes `Adobe Premiere Pro *` and `*.PRV` subdirs | REASONING: Premiere generates `.mp4` preview renders inside its project folders — caused 585 fake "clips" in the index when Gray left a Premiere project inside `02_ORGANIZED/2026-04-27/`. Walker now skips those names defensively. | CONTEXT: Also pruned 585 stale rows from existing SQLite. Independent of where Gray stores Premiere projects — the indexer is now safe even if a project gets dropped in ORGANIZED by mistake.

[2026-04-29] DECISION: Footage organizer ORGANIZED + FOOTAGE_LIBRARY will use the same `<category>/<date>/clip.mp4` shape; format folder dropped, format becomes index-only metadata | REASONING: Currently ORGANIZED is `<date>/<format>/<category>/` (date-first) and FOOTAGE_LIBRARY is `<category>/<date>/` (category-first). Gray hunts for b-roll category-first ("do I have any candid-people clips lately?") — date-first ORGANIZED forces the wrong drill order. Unifying makes archive a structure-preserving move and matches the way Gray actually uses the library. | CONTEXT: Plan at `plans/2026-04-28-footage-organizer-flatten-format-folder.md`. In-progress as of 2026-04-29; partially implemented (Steps 0-3 of 8) and blocked on a paused interactive rebase. Format query still works via `pull --orientation`/`--format` against SQLite — just not as a folder hierarchy.

[2026-04-29] DECISION: Day-of-flat rule will be enforced in code via `--allow-today` escape hatch, not convention only | REASONING: Today's dump folder is the day-of editing scratchpad — accidentally categorizing it would scatter your editing files mid-edit. Code-level guard prevents that destruction class entirely; one-flag override when you actually do want to categorize same-day. | CONTEXT: Adds `--allow-today` to main.py argparse; categorize logic compares date_str to today and errors if equal without the flag. Implementation pending Step 3 of the flatten refactor.

[2026-04-30] DECISION: Edit 4K natively on the Windows rig — skip proxies for ~95% of work | REASONING: RTX 5070 (Blackwell) has dual NVDEC with hardware 4:2:2 10-bit HEVC decode — the codec class that historically forced "always proxy" workflow on Sony FX3 / A7S III / Canon R5 footage. Combined with the 14900K's 24 cores, 64GB DDR5, and 7,000 MB/s NVMe, native 4K is smooth for single-stream timelines, light multicam, color grading, and most effects stacks. | CONTEXT: Still proxy for 3+ angle multicam of 4K HEVC 4:2:2, RAW workflows (RED/BRAW/ProRes RAW), heavily layered effects (multiple Warp Stabilizers / AI features), or 8K. For Gray's daily Sai shorts + LinkedIn cuts + weekly long-form, native is the default. Verify Mercury Playback Engine = GPU Acceleration (CUDA) and Premiere is on 2024.5+ for full Blackwell support.

[2026-04-30] DECISION: Mac (2022 M2 / 8GB) is light-duty only — never recommend for renders, Whisper, or parallel jobs | REASONING: 8GB unified memory is the bottleneck — a single 4K timeline swaps aggressively, Whisper local transcription thrashes to disk, and any GPU-bound workflow is dramatically faster on the Windows i9 + RTX 5070. Gray's own assessment: "shit for editing." | CONTEXT: Mac role: brainstorming with Claude, terminal/Python work, Slack, GitHub, travel, phone-shot rough cuts in CapCut. Windows role: ALL editing, renders, transcription, parallel work, Sai deliverables. Cross-machine sync is git-only — Mac-Claude memory and conversation history don't reach Windows-Claude, so brainstorms paused on Mac must produce a plan/decision/doc and push before session ends.

[2026-04-30] DECISION: Built ad-hoc AI dual-cam editing pipeline directly in `D:\Sai\<shoot>\edit\` (no new python-scripts/ project) for the Sai long-form A-roll/B-roll auto-cut | REASONING: One-off workflow proven end-to-end (audio xcorr sync → Whisper word-level on RTX 5070 → Claude picks best takes → snap to word boundaries → render parallel ffmpeg). Per build-discipline rule 1, the bar to spin up a new tool is high — Content Pipeline already does transcription, ffmpeg + scipy + anthropic SDK glue is short-form Python. If Gray runs this 3+ more times on long-form Sai shoots, THEN promote to `python-scripts/dual-cam-cutter/` with a CLI. For now: artifacts (transcript.json, edl.json, claude_keep.json) live alongside the source for re-rendering. | CONTEXT: 18:40 source → 9:39 final, both A-roll (1080p @ 23.976fps) and B-roll (1080p @ 25fps) at exactly 578.78s with frame-locked cut points. Sync offset: B started +51.57s after A — first 51.57s of A dropped from both to preserve the same-length sync guarantee. 84 keep-segments out of 116 phrases. 30ms audio fades + 50/80ms padding at every boundary. Total wall time ~5 min including Whisper.

[2026-04-30] DECISION: Use Remotion Studio (not HyperFrames) when Gray wants a browser dashboard to edit clip data live | REASONING: User asked for "edit text and clips on a dashboard." Remotion's Zod-schema-driven Props panel is the closest match — array editing with add/remove/reorder, text inputs, color pickers, number sliders, all live-updating the preview. HyperFrames has a Studio for preview/lint but it's playback-focused, not a per-clip param editor. | CONTEXT: Scaffolded at `web-apps/remotion-sai-vlog/` with two compositions (`DayInLife` + `DayInLife-NoTimestamps`), zod schema covering showTimestamps/timestampColor/fontSize/clips array, 21 trimmed segments copied to public/clips/ (gitignored, 54MB), default props read from clips.ts. Studio runs at localhost:3000 via `npm start`. Caveat: Remotion's "Open in editor" button is hardcoded to reject paths with spaces — the "Gray Davis" username trips the Windows whitelist. No flag to disable. Workaround: open .tsx files in VS Code directly (hot-reload still works fine).

[2026-04-30] DECISION: Vertical day-in-life montage uses skin-exposure (HSV) + frame-diff motion as the per-clip window picker; orange `#F28129` timestamp label = clip's actual capture time from `creation_time` metadata | REASONING: Gray wanted "best 4-5 sec where skin is exposed correctly and footage is most stable." HSV skin detection (H<35 or H>240, S 30-160, V 50-240) + ideal mid-tone score gives a 0-1 exposure score; mean abs frame-diff at 2fps gives a motion penalty. Combined: score = exposure - 0.6 * motion. Picks lowest-motion well-exposed window per clip with 0.5s sliding step. Walking clips score uniformly = doesn't matter which window wins (all good); static-camera clips with brief shake correctly favor the steady moment. Capture-time timestamp because that's what the reference video does (chronological day flow shown via clock). | CONTEXT: Algorithm in `D:\Sai\AI Edits\2026-04-30\analysis/` (per-clip 2fps frames + plan_v2.json with scores). For tougher cases (vidstabdetect transform energy is the proper camera-shake metric), can swap in next pass — same speed, more accurate. Suggested upgrade noted but not implemented.

[2026-05-01] DECISION: Sai-revised LinkedIn captions go into `python-scripts/sai-linkedin/reference/voice/sai-linkedin-posts-final.md`, AND specific revision patterns get encoded as AVOID rules in SYSTEM_PROMPT_CORE | REASONING: First production run of sai-linkedin (2026-04-23 "Do Over" post) produced a draft Sai revised in 8 distinct ways — em-dashes cut, AI-essay headers replaced, poetic verbs flattened, declarative claims hedged, etc. Two-layer feedback: (a) cached corpus learns Sai's voice via pattern-matching, (b) explicit AVOID rules catch the failure modes harder. Both compound — corpus addition is free per run (cached); SYSTEM_PROMPT_CORE growth from ~6.2K to ~7.2K chars adds ~$0.005/run cost, trivially offset by fewer revisions needed. | CONTEXT: 8 patterns documented in memory `feedback_sai_linkedin_voice_revisions.md`. Watch next 2-3 runs for over-correction (rule overload → mechanical output); fix would be tightening rules, not adding more. SYSTEM_PROMPT_CORE itself still uncached — adding cache_control on the core block is a one-line follow-up if cost ever matters.

[2026-05-01] DECISION: Long-form dual-cam pad defaults are 0ms head + 400ms tail, snapped to Whisper word boundaries; outputs forced to common fps + 48kHz audio when source cams differ | REASONING: 04-30 v1 used 50ms head / 100ms tail which chopped the last syllable of trailing words (Whisper word-end timestamps run a hair early) and left dead air at clip starts. Re-render with 0ms head / 400ms tail fixed both — tighter cut-ins, last word + breath finishes naturally. Frame-rate lock matters: A-cam C2313 = 23.976fps, B-cam = 25fps; without forcing both segments to 24000/1001 + 48kHz audio, drift accumulated 0.78s across 79 cuts (broke multicam frame-lock in Premiere). | CONTEXT: Re-render uses saved artifacts (`edit/claude_keep.json` + `edit/words.json` + `edit/sync.json`) — no Whisper or Claude re-pay. Script lives at `python-scripts/multicam-mirror/longform_rerender.py`. Output: `D:/Sai/AI Edits/2026-04-30/long-form-v2/final_Aroll.mp4` + `final_Broll.mp4`, both 529.59s, frame-locked to within 0.25ms. 5 tail picks dropped + 1 clamped because B-cam stopped recording 54s before A. If padding feels wrong on future runs, change `HEAD_PAD` / `TAIL_PAD` constants at the top of the script and re-run.


## 2026-05-02 — Footage Organizer index uses relative paths for cross-machine compat

The SQLite index (`.footage-index.sqlite`) at the library root now stores
clip paths relative to the library root (`05_FOOTAGE_LIBRARY/...`) in POSIX
form, instead of machine-absolute strings. Resolution to absolute happens at
read time using the current machine's `<CLIENT>_LIBRARY_ROOT` env var.

**Why:** `D:/Sai` on Windows is the same physical exFAT SSD as
`/Volumes/Footage/Sai` on Mac. Storing absolute paths meant the index was
single-machine — switching machines made `Path(p).exists()` return False for
every row, `remove_missing()` deleted them all, and `pull` returned zero
results until re-indexed.

**Migration:** symmetrical + automatic. `cli_index._check_legacy_db()` detects
absolute paths in the existing DB and wipes the `clips` table; the next
`index` run repopulates with relative paths. No manual migration step. First
run on either machine after the upgrade triggers it; subsequent runs on
either machine short-circuit.

**Why this is safe for the existing Mac workflow:** The `clips` table is the
only thing wiped. `.cache.json` (vision-classification cache) is unaffected
— its keys are filename + filesize and were already portable. The vision
cache prevents any paid Claude API re-classification; rebuilding the index is
just ffprobe + sha1 of the first 1 MB per clip, ~30 seconds for 238 clips at
zero cost.

[2026-05-02] DECISION: Manual-edit + Claude-asset-generation is the locked workflow for Sai long-form (NOT collaborative timeline editing) | REASONING: Tried collaborative AI editing (custom EDL tools, Remotion, multi-cam pipeline). Friction was high — Claude's creative-judgment errors cost more time than they saved. Gray edits faster and with better creative judgment than any model. Asset generation is where Claude actually adds value (deterministic, parameterized, instant N-variations). | CONTEXT: After Gray manually edited C2313.MP4 down to 6:43.5 vs my AI v4 at 10:51, the diff exposed how much over-inclusion the take-selection model produces. Not fixable by better prompting — fixable by changing who does what. Workflow saved in `~/.claude/.../memory/workflow_sai_video_assets.md`.

[2026-05-02] DECISION: For templated/recurring video assets, prefer PIL+ffmpeg over After Effects | REASONING: PIL+ffmpeg gives pixel-identical N-variations from one parameterized script (lesson cards 1/2/3 = swap NUMBER + TEXT + SLOT_NAME, re-run, done in 2min). After Effects requires per-card manual tweaking, no version control, no batch generation. AE still wins for one-off creative animations, footage compositing, MOGRT templates Gray reuses across multiple Premiere projects. | CONTEXT: First proof in `D:/Sai/08_AI_EDITS/long-form/A-Roll Long Lessons.../edit/assets/lesson_cards/build_lesson_card.py` — produces ProRes 4444 .mov with alpha (transparent numeral cutout, no chroma key needed in Premiere). Hybrid going forward: Claude renders the templated chrome, Gray uses AE for the creative one-offs that deserve manual craft.

[2026-05-03] DECISION: Two short-form Graydient content formats, not three (Growth Recap weekly + "Day X creative director" weekly). Monthly recap killed. | REASONING: Three formats fragments weekly output and dilutes serialization. Monthly reflection has more room inside the long-form YouTube vlog. Shorts reward serialization — the "Day X" counter does the milestone job ("Day 30 — first month done") without needing a standalone format. | CONTEXT: Locked during 2026-05-03 session. Gray at 328 total followers (IG 153, TT 165, YT 10), goal 10K by end of summer. Day 1 = 2026-04-16 (Sai start). Format spine: 45s, iPhone-in-room, B-roll/screen-rec overlays, hook in first 3s, same opener every week.

[2026-06-02] DECISION: Added main-branch guard to safe-session-commit.sh — auto-commit now aborts unless HEAD is `main` | REASONING: A 23-day-old paused rebase parked Windows sessions on snapshot-2026-05-29-windows (detached HEAD). Every Stop pushed to that branch, not main. ~60 HyperFrames composition dirs, content-OS SOPs, thumbnail research, sfx-library, sai-thumbnail, day-x-study landed on snapshot branches and never reached origin/main. Existing bulk-deletion threshold never tripped because nothing was deleted — work was silently being committed to a sibling lineage. | CONTEXT: Same script also now backs a SessionStart warning in ~/.claude/settings.json that prints a stderr notice if the working branch isn't main. Recovery commit 987caac restored 762 files to origin/main from the freshest snapshot branches; snapshot branches then deleted locally + remotely.

[2026-06-02] DECISION: Plug TikTok token leak — untracked `python-scripts/social-media-analytics/tiktok_token.json` and broadened the gitignore pattern from `token*.json` to also include `*token*.json` | REASONING: The committed file contained a real (but already-expired/invalid) TikTok access_token + refresh_token. Old pattern only matched filenames starting with `token`, so `tiktok_token.json` slipped through. | CONTEXT: Tokens were dead at time of discovery (file's own error field confirmed `Refresh token is invalid or expired.`), so rotation not strictly required — but the strings are in git history. Same lesson applies to any future `<prefix>_token.json` file.

[2026-06-02] DECISION: Point Obsidian MCP at the Drive vault, not the deleted Documents vault | REASONING: ~/.claude/settings.json's mcp-obsidian arg was still `C:/Users/Gray Davis/Documents/Obsidian/Graydient Media` — that vault was consolidated to Drive 2026-05-08 but the MCP arg never updated, so it was reading a dead path. | CONTEXT: Updated to `C:/Users/Gray Davis/My Drive/Obsidian/Graydient Media`. Documents/Obsidian/ still has orphan content (Business/, Content/, Ideas Backlog.md, Inbox.md, Scripts.md, plans/) flagged "to triage" — left untouched pending Gray's decision.

[2026-06-02] DECISION: One canonical `scriptwriter` subagent (`.claude/agents/scriptwriter.md`), not two. A Mac-built `sai-script-writer.md` (Sai-only, ad hoc) was deleted in favor of the Windows-approved design spec (`docs/superpowers/specs/2026-06-02-scriptwriter-subagent-design.md`), with the Sai-shorts voice rules merged into it. | REASONING: Two overlapping agents = drift. The spec's version is broader (Gray+Sai switchable, 4 input modes, WebSearch+Shock-Score, all formats) and was already approved. Built directly from the spec rather than the formal writing-plans→/implement flow because it's a single agent file and the design was locked. | CONTEXT: Mac + Windows built scriptwriter work in parallel on 2026-06-02; reconciled during the Mac merge of the Windows lineage-recovery commits.

[2026-06-02] DECISION: For Sai SHORTS, the scriptwriter uses Sai's voice-MEMO voice, never his LinkedIn voice. LinkedIn corpus is only for writing LinkedIn posts. | REASONING: LinkedIn/newsletter corpus is *written* (polished) voice — pulling its rhythm is the #1 cause of AI-flavored shorts. Shorts must sound like the looser voice-memo Sai. The Windows design spec had over-generalized to the LinkedIn corpus; corrected. | CONTEXT: Consistent with the long-standing rule in `sai-weekly-script-batch.md` + `sai-script-style-guide.md`. The agent reads voice-memo transcripts + the style guide for Sai.

[2026-06-02] DECISION: Two distinct tiers of Sai shorts. (1) Batch/produced shorts — graphics + effects, Saturday→Monday pipeline, covered by `workflows/sai-batch-video-production.md`. (2) UGC shorts — strip-down, no graphics/B-roll/music, Sai films + edits himself, covered by `business/sai-karra/content-os/` per-platform SOPs. | REASONING: Resolves the apparent contradiction between the content-os hard rule "no HyperFrames on daily UGC shorts" and the batch shorts that DO get graphics. They're different tiers, not a conflict. | CONTEXT: Gray confirmed 2026-06-02.

[2026-06-02] DECISION: Frame rate — only HyperFrames graphics render at 60fps; A-roll trim, captions, and final delivered video stay at 24fps (23.976). | REASONING: 60fps graphics can be speed-adjusted in Premiere without quality loss; the rest of the pipeline matches the 24fps source. Fixed the editing SOP's Step 3 graphic-render line (was `--fps 24`, now `--fps 60`). | CONTEXT: Reconciled the 60fps motion-graphics-batch default against the editing SOP's 24fps pipeline.

[2026-06-02] DECISION: Renamed `content-pipeline` → `transcriber` and stripped it to transcription-only (removed moment_picker / caption_writer / video_cutter). | REASONING: Gray confirmed the clip-picking half was never used — the real shorts flow is Sai films → Gray edits manually → sai-captions. Only the transcription + voice-memo-notes half earns its keep, so the name now matches the one job. | CONTEXT: Updated the skill, workflow doc, README, personal-assistant tool_registry, project-status.json, and sai-linkedin's .env hint. Historical plans/audits keep the old name (point-in-time records). ANTHROPIC_API_KEY made lazy so a plain transcript needs no keys.

[2026-06-02] DECISION: Archived `hook-optimizer` and `founders-series` to `python-scripts/_archive/`. | REASONING: hook-optimizer was never used and the short-form story-arc playbook now covers hooks; founders-series helper is no longer used (the Founder Series content SOP stays). | CONTEXT: hook-optimizer skill moved out of `.claude/skills/` so it no longer auto-fires. Both removed from the personal-assistant registry and project-status.json.

[2026-06-02] DECISION: `content-researcher` repositioned as a GRAYDIENT-BRAND tool (find topics for Gray's own content), not a Sai-SOP tool. | REASONING: The story-arc playbook covers HOW to structure/hook a video; content-researcher covers WHAT topic to make. For Sai, topics come from his voice memos, so the researcher is mainly useful for growing Gray's own brand. Only used on Sai work when Gray specifically asks. | CONTEXT: Skill description updated to reflect primary use.

[2026-06-02] DECISION: Whisper stays a Windows-only install; not installed on the Mac. | REASONING: ~95% of editing (and all batches) happen on the Windows PC where Whisper runs fast on the RTX 5070 GPU. Installing it on the Mac (and fighting Python 3.14 + torch) is wasted effort. | CONTEXT: The transcriber + sai-captions tools run on Windows. sai-linkedin paused for now.

[2026-06-02] DECISION: Register the Sandcastles MCP directly in `~/.claude.json` rather than installing the vendor's Claude Cowork plugin. | REASONING: The VS Code extension runs with `--strict-mcp-config` and only reads `~/.claude.json`, so plugin-bundled `.mcp.json` servers are ignored in Gray's environment. Direct registration (matching the Higgsfield http/url pattern) is the only path that actually connects. Copied the plugin's 14 slash commands to `.claude/commands/sandcastles/` separately so the `/sandcastles:*` UX still works. | CONTEXT: Sandcastles MCP integration; plugin source archived at references/sandcastles-claude-plugin/.

[2026-06-02] DECISION: Build Sandcastles watchlists at 25/list (Gray + Sai) with niche-fit as the hard gate and follower-size as a soft sort; fill nano/mid tiers via the tool's own `discover_channels` rather than from model memory. | REASONING: Pro plan caps ~50 channels total, so 30/30 doesn't fit. Outlier signal is most attributable/replicable at nano/mid (macro outliers are confounded by audience), so weight 10 mid / 9 nano / 5 macro + own channel. Inventing small-account handles from memory would produce wrong/dead accounts — channels-search returns real, currently-active accounts with real data. | CONTEXT: Plan saved at business/sandcastles-watchlists.md; macro tier seeded from creator-intel craft creators (short-form accounts only).

[2026-06-03] DECISION: Scriptwriter system = umbrella term for story-arc-playbook + scriptwriter subagent + sai-weekly-script-batch SOP | REASONING: First production run (Sai Batch 2) exposed that the three components don't have a unified name and Gray needed one. "Scriptwriter" alone refers to the subagent only. | CONTEXT: Three pieces — knowledge base (`business/social-media/story-arc-playbook/`), operator (`.claude/agents/scriptwriter.md`), batch procedure (`workflows/sai-weekly-script-batch.md`). Use full names when referring to a specific piece.

[2026-06-03] DECISION: Merge multi-part voice-memo recordings at the transcript level before scriptwriting | REASONING: Sai's "Part 1 + Part 2" filenames in Batch 2 were recording-interrupt continuations (he ran out of recording space and restarted), not separate ideas. Treating them as separate scripts would have split one coherent thought into two thin ones. | CONTEXT: Implemented in this run via a Python merge step — pt 1 + pt 2 transcript bodies concatenated under one combined transcript file, originals archived to `_merged-sources/`. Pattern likely applies to any future `<topic> part 1` + `<topic> part 2` pairing. v2 candidate: detect + merge automatically.

[2026-06-03] DECISION: Scriptwriter subagent must flag every invented line for verification before filming | REASONING: Batch 2 Script 8 ("Landed a Fortune 500 client") closer "We won." was invented — transcript heavily implied the win but never stated it. Subagent surfaced it in self-audit. Without a structured verification flag, invented copy could slip through. | CONTEXT: v2 candidate #14 in the retro — subagent should return a structured `verify_before_filming` list of any lines it added that weren't in the source transcripts. Gray confirms each before locking the batch.

[2026-06-03] DECISION: The Graydient Sandcastles watchlist uses PRODUCTION-FIT as the hard gate — "could I film + edit a video like theirs in a few hours, today?" — ranked above follower-size and even niche-fit. | REASONING: Gray's main job is Creative Director for Sai; Graydient Media only gets leftover time/energy, so it must be low-lift same-day content. Studying cinematic/effect-heavy creators is actively harmful — their hooks lead to edits Gray has no time to produce. Skew toward AI-tool/screen-record demos, talking-head editing tips, and small/emerging creators (who are forced into low-lift formats anyway); cut cinematic. | CONTEXT: Drove the v1→v4 watchlist curation; mirrors Sai's UGC strip-down philosophy.

[2026-06-03] DECISION: On the Pro plan, run a SINGLE Sandcastles watchlist (Graydient) in the one workspace; handle Sai's research via global tools (search_all_videos + a saved set of Sai channel UUIDs) rather than a second watchlist. | REASONING: Pro allows only one workspace (no create-workspace MCP tool; multi/guest workspaces are a Titan-only feature). Mixing Gray's videography niche + Sai's founder niche in one watchlist would wreck the watchlist-scoped tools (topics/hooks). The UUID-filter approach gives Sai a functional "list" without polluting Gray's. | CONTEXT: Upgrade to Titan only if Sai's content later justifies a dedicated tracked workspace.

[2026-06-04] DECISION: When captioning an AI-assembled cut, reuse the source's existing word-level transcript mapped onto the cut timeline using REAL encoded per-segment durations as offsets — do not re-transcribe, and do not use theoretical (end-start) offsets. | REASONING: Re-encoding each segment at a fixed fps quantizes durations to whole frames, so summing the intended (end-start) lengths drifts from the actual concatenated timeline (~286ms over 15 segments on Batch 2 Vid 1). Probing each encoded clip's real duration and accumulating those gives drift-free caption offsets, AND reusing the already-paid ElevenLabs Scribe transcript beats re-running a local CPU model (faster_whisper wasn't even installed). Intra-segment word timing is exact since words are continuous from the source. | CONTEXT: Implemented as caption_from_scribe.py for the B2;V1 edit; stubbed faster_whisper in sys.modules to import sai-captions/caption.py's rendering functions without its ASR dependency.

[2026-06-04] DECISION: Sai short-form raw footage shot on the Sony (XAVC) can arrive as stored-landscape 1920x1080 + rotation=90 metadata (displays vertical 1080x1920). Always check rotation before editing, bake it per-segment (autorotate -> scale/pad 1080x1920), and DON'T use video-use's render.py on it. | REASONING: render.py hardcodes scale=1920:-2 which assumes landscape; on a post-autorotate 1080x1920 frame it would upscale to 1920-wide and break the vertical. A custom per-segment extract that respects autorotate and normalizes to 1080x1920 is required. | CONTEXT: B2;V1 source; rotation confirmed via ffprobe displaymatrix (rotation=90). Sai shorts are vertical even when the camera stores landscape.

[2026-06-06] DECISION: Fixed a sign bug in `multicam-mirror/sync.py` (`compute_offset`) that placed every mirrored B-cam segment at A − offset instead of A + offset. | REASONING: The function negated the cross-correlation lag (`offset_s = -lag/sr`), returning tA−tB while its own docstring promises tB−tA ("seconds to add to an A timestamp to get B"). main.py does `b_start = start + offset`, so the negation desynced the B reel by exactly 2× the true offset (caught via end-to-end audio cross-correlation of final_a vs final_b: a constant −5.79s = −2×2.895s on the pod's video 2). Removed the negation; re-verified A/B align at 0–1.4ms across start/mid/end of all reels. | CONTEXT: First real validation of multicam-mirror main.py+sync.py (prior dual-cam work used the separate longform_rerender.py). Found during the Longform Pod multicam sync job.

[2026-06-06] DECISION: `multicam-mirror` now locks each mirrored B-cam segment to the A-cam's exact framerate (`-r <a_fps>`). | REASONING: A-cam was 23.976fps (Sony), B-cam 25fps (Canon). Without the lock, each segment rounds to a different frame count per camera and the reels drift apart (~0.23s over 32 cuts → broken lip-sync by the end). Locking B to A's r_frame_rate keeps per-segment frame counts identical; measured drift dropped to <0.03s (sub-frame). Set the Premiere sequence to the A-cam framerate. | CONTEXT: Added `get_fps()` + an `fps` param to `extract_segment()`; README updated. Same framerate-lock lesson as the earlier longform_rerender 24000/1001 fix.

[2026-06-05] DECISION: Built a dedicated `footage-puller` subagent (`.claude/agents/footage-puller.md`) instead of doing footage content-searches in the main session. | REASONING: Finding clips by subject requires SQLite queries + ffprobe rotation probes + extracting/viewing frame contact sheets — all token-heavy and noisy in the main context. Isolating it in a subagent keeps the main session lean and lets the agent permanently hold the two non-obvious gotchas: (a) the index has NO content/subject column (search by sight via contact sheet), and (b) `pull --orientation vertical` is unreliable because most footage is Sony 1920×1080 + 90° rotation flag (displays vertical, reads horizontal) — must ffprobe tags=rotate/side_data=rotation. | CONTEXT: Surfaced during a session where pull returned only 4 of 447 clips as "vertical" and the street video Gray wanted was mislabeled in establishing-exterior. Agent's first run found the red-light bed-journaling clips (C2415–C2419), 3 of which were mislabeled insert-hands.

[2026-06-06] DECISION: Sai HyperFrames graphics use drawn SVG infographics, NOT emoji. | REASONING: Emojis break the locked Sai-Shorts brand system (white + orange `#F28129` line-art on chroma green per `web-apps/hyperframes/*/design.md`/`DESIGN.md`) — full-color emoji clash with the palette, can carry green-ish pixels that fight the Ultra Key, and don't match the line-art house style. When Gray asked for "⚡🚒🚑 → person" on the ig-6-reframe card, the emoji version looked off and was rebuilt as white-stroke/orange SVG icons (fire truck, ambulance, police car, person). | CONTEXT: Reusable rule for any Sai HyperFrames composition; the brand MD is the template Gray references ("the MD file we've been using"). Drove the firesirens ig-6-reframe rebuild.

[2026-06-06] DECISION: Render HyperFrames graphics as alpha ProRes 4444 (`--format mov`, transparent background) — not chroma green — whenever the graphic uses drop shadows. | REASONING: A soft drop shadow over green is a dark+green pixel mix that an Ultra/chroma key can't cleanly separate, leaving a green/dark fringe around the shadow. Alpha output carries a true matte so the shadow composites perfectly over any footage and Gray just drops the .mov in (no keying). Flat graphics with no shadow stay on green per the batch convention. | CONTEXT: Hit during the Vid 4 sleep/siren stick-figure scene when Gray asked for drop shadows on the figure + bell; the green preview fringed, switching to alpha fixed it. Kept a green-screen MP4 too since Gray explicitly wanted one.

[2026-06-06] DECISION: Batch 2 per-video HyperFrames graphics export to `D:/Sai/06_ASSETS/Visual Effects/Batch 2/Vid <N>/` (nested Batch 2 → Vid N folders), NOT the older per-package path like `Batch 2 Vid 7 - Identity NFTs/` written in sai-b2v7-nfts/DESIGN.md. | REASONING: Gray explicitly asked for a `Batch 2` folder with a `Vid 7` subfolder and wants all of a video's graphics (across separate composition projects) to land together there. | CONTEXT: Both Vid 7 graphics this session — stick-crowd-blend.mp4 (green) and nft-pouring-resources.mov (alpha) — went to `Batch 2/Vid 7/`. DESIGN.md still lists the old path; treat the nested convention as canonical for future Batch 2 exports.

[2026-06-06] DECISION: Vet imported screenshots/images for chroma-key-matching colors before using them in a green-screen HyperFrames comp | REASONING: In `hyperframes-nft-crash-stack` (over chroma green #00B140), the TechCrunch article screenshot had a solid brand-green diagonal block that keyed straight through during chroma key — you could see through the card. Removed article-03-techcrunch → 5 articles. | CONTEXT: General rule for any composited screenshot asset — if it contains green near the chroma value it will key out as a hole. Saturated brand greens (TechCrunch) are the danger; pale teals (The Verge wordmark) are borderline. Pre-flag green content before stacking.

[2026-06-06] DECISION: Captions must never flicker at the line change — render them gapless, full-opacity, hard-cut between cards (no fade-out, slide-up, or crossfade entrance). | REASONING: Gray flagged a "little flicker" right before each line transition. Frame analysis of the batch caption animation showed the fade+16px-slide+crossfade entrance dropped the text to ~77% brightness for ~3 frames on every card change — that dim-and-shift reads as a flicker. A hard cut at full opacity (each card holds until the next card's start) keeps max luma pinned at 255 across transitions, so there is nothing to flicker. | CONTEXT: Fixed in both D:/Sai/01_ORGANIZED/Batch 2/_b2_edit/recaption_final.py and the batch _b2_edit/pipeline.py; locked as a hard rule in workflows/caption-standards.md + memory feedback_caption_no_flicker.md (with a verify step: sample consecutive frames over black, max luma must stay 255).

[2026-06-06] DECISION: To re-caption a video after Gray finishes the Premiere edit, transcribe his FINAL exported audio directly (local Whisper large-v3) instead of reusing/remapping the original paid Scribe transcript. | REASONING: Once Gray picks a hook and applies hand-trims in Premiere, his final cut's timeline no longer matches the batch cut the original caption layer was built against (B2V7 final = 60.1s vs the 93s both-hooks cut). The only source of truth for his final edit is the exported audio itself. Local Whisper on the RTX 5070 is free and accurate enough, so there's no need to re-pay ElevenLabs. | CONTEXT: New reusable script recaption_final.py; produces a house-style alpha caption layer synced to the final audio. First use: B2V7 "Identity Equals NFTs".

[2026-06-06] DECISION: A Sai HyperFrames card that has any GREEN visible element (e.g. a green checkmark or a green card-blink) must be delivered as a ProRes 4444 alpha .mov, never a green-screen MP4. | REASONING: The cards render over chroma green #00FF00 and are normally keyed in Premiere with Ultra Key. A green checkmark/flash would be keyed out along with the background, leaving a hole. Alpha render avoids keying entirely, so green elements survive. | CONTEXT: Came up building sai-b2v7-nfts/ig-3-lesson (green ✓ + green card-blink). DESIGN.md already specified ig-3 as alpha for its glow/shadows, so this was consistent.

[2026-06-10] DECISION: Sai HyperFrames infographics shift to a cinematic "AE-3D" motion-graphics style — camera pan/zoom via a transformed "world" div (transform-origin 0 0; tx = framecenter − scale·worldpoint to frame a target), objects moving through space at constant speed (ease "none"), takeoff/fly-off+fade beats — over flat pop-in cards | REASONING: Gray reacted strongly to the b2v5 airplane-runway (plane taxis up → camera pan-right → zoom into "6 MONTHS"), called it "fucking fantastic" and said he's making more graphics like it; becoming his signature for Sai infographics | CONTEXT: Saved to memory feedback_prefers_3d_motion_graphics.md; render alpha ProRes 4444, camera-heavy ones may warrant 60fps (the skyscraper collapse was 60fps)

[2026-06-09] DECISION: Refine the caption no-flicker rule — the flicker culprit is the CROSSFADE overlap/dip, not the fade-in itself. New rule: use a clean ease-in fade-in ONLY when a card appears over a BLANK screen (the open, or after a real speech pause), and HARD-CUT at full opacity between contiguous cards during continuous speech. | REASONING: Gray asked for a "smooth ease-in / fade-in" on B2V5 captions, which seemed to conflict with the 6/06 "hard-cut, no fade at all" rule. But that rule over-corrected: the actual flicker was the crossfade OVERLAP (outgoing + incoming both partially transparent → text dimmed to ~77% for ~3 frames). A fade-in from a blank screen has no overlap, so it can't flicker. The one trap is fading in EVERY card — back-to-back cards drop to 0 then ramp ~4 frames, which is its own flicker; so contiguous cards must hard-cut. Limited-range luma trace of the new B2V5 layer: 1536 frames, 0 unwanted dips (bright→dim→bright without a real pause), 14 intentional ramp frames at pauses. | CONTEXT: Implemented in D:/Sai/01_ORGANIZED/Batch 2/_b2_edit/recaption_smooth.py (added a per-card fade_in flag = card is i==0 or prev card faded out). REFINES the [2026-06-06] hard-cut decision. NOT yet locked into workflows/caption-standards.md + memory feedback_caption_no_flicker.md — waiting on Gray's OK of the B2V5 preview before overwriting the existing hard-cut standard.

[2026-06-10] DECISION: Caption-project videos (source clips + burned-in caption renders) are DELETED from the repo, not stored on the Footage SSD. | REASONING: Gray confirmed the renders are already posted to social and he has the raw footage; the repo source.mp4 is a duplicate (raw camera originals live in D:/Sai 01_ORGANIZED/Batch 1 C24xx.MP4 + the uncaptioned cut in 03_DELIVERED/shorts). He explicitly does not want footage duplicated onto Footage. So no SSD copy, no week-buffer, no auto-purge job — just remove them. Only the HyperFrames/captions CODE stays in git. | CONTEXT: Removed 8 tracked files (~315MB) from sai-vid2/sai-vid3 caption projects (commit f28c1cd). Already enforced going forward by the existing .gitignore (*.mp4 + **/renders/) — no gitignore edit needed. Resolves the old "move caption videos to SSD" TODO (the move plan was superseded by delete).

[2026-06-10] DECISION: The b2v5 "Emergency Fund" airplane-runway is the ⭐ FLAGSHIP / benchmark animation for Sai infographics — the standard for our most advanced + engaging motion graphics; prioritize making more like it to keep viewers watching | REASONING: Gray explicitly starred it as one of the most important animations and what advanced animations should look like; cinematic camera-driven pieces are far more intriguing/engaging than flat pop-in cards | CONTEXT: Recipe — transformed "world" camera (transform-origin 0 0; tx = frameCenter − scale·worldPoint to frame a target); SYNC pan(x) with zoom(y+scale) in ONE ease so the framed target glides monotonically to center — letting the pan lead the scale causes the target to overshoot/swing past then come back; plus constant-speed object motion + fly-off-and-fade. Marked in memory feedback_prefers_3d_motion_graphics.md + b2v5 DESIGN.md

[2026-06-11] DECISION: The dead-air silence-trim is a HARD-CODED rule in the batch pipeline (trim_silence.py, auto-run by pipeline.py), NOT a memory note or a sub-agent. | REASONING: Diffing Gray's Premiere final vs the AI cut on Vid 8 "Fortune 500" showed the AI left a 10.3s silent hole he removed by hand (41% total reduction, ~matching May 3's 42%). Silence removal is purely mechanical (a dB/duration threshold), so it belongs in deterministic code that runs identically every time — not in a "remember to" memory (soft, recall-dependent) and not in an LLM sub-agent (costs tokens, non-deterministic, less reliable than math). Split: mechanical edits → code; judgment edits (which hook is punchier, bad-take detection) → memory for the selects prompt. | CONTEXT: trim_silence.py collapses gaps ≥0.30s (0.10s breath each side), applying identical frame-snapped keep-ranges to the cut AND the caption alpha so they stay synced. Verified on the Vid 8 cut: 56.5→42.9s, residual silence none, A/V 5ms, speech preserved. Standalone + reusable; pipeline.py --no-trim skips. See feedback_sai_short_form_ai_edit_lessons.md Rule 8.

[2026-06-11] DECISION: For the b2v8 Vid 8 ig-3 graphic, scrapped the "contract race" (perspective race-pods) and rebuilt it as a "Spotlight pick" — 3 big steel "agency" towers + Sai's small orange one; a stage spotlight scans the lineup (per-tower brightness glints as it passes) and locks on the orange underdog (ignite + rise + WON), losers dimmed via dark veils, then a synced camera push-in. | REASONING: The race-pods read poorly — couldn't tell which marker was Sai ("hard to correlate the two"). The spotlight makes "3 bigger competitors, the client chose the small one" instantly legible. Gray reacted very strongly ("fucking fantastic… more effects like this please"), so light-driven storytelling (spotlight scan + per-object brightness reveal + loser-dimming) is now a go-to pattern for "chosen / stood out vs others" beats — a 2nd starred exemplar alongside the b2v5 airplane camera move. | CONTEXT: web-apps/hyperframes/sai-b2v8-fortune500/ig-3-contract-race/index.html; rendered 60fps alpha to D:/Sai/06_ASSETS/Visual Effects/Batch 2 Vid 8 - Fortune 500 Clients/Contract Race - Spotlight.mov; saved to memory feedback_prefers_3d_motion_graphics.md (2nd exemplar).

[2026-06-11] DECISION: Use the extract-each-range-then-concat method for the silence-trim, NOT the ffmpeg select/aselect filter. | REASONING: The one-pass select filter cuts video on frame boundaries but audio on sample boundaries, so the two streams accumulated ~0.26s of drift across 11 cuts on the Vid 8 cut (audio ran ahead = lip-sync off by ~¼s by the end). Extracting each keep-range with -ss/-t (re-encoded, audio+video aligned from one seek point) then lossless-concatenating — the same method pipeline.py's build_cut already uses — produced 5ms A/V diff (sub-frame). Frame-snapping the keep-range boundaries also helps (at 24fps/48kHz that's exactly 2000 samples/frame). | CONTEXT: trim_silence.py _apply(). Verified A/V stream durations match within 5ms on the trimmed Vid 8 output.

[2026-06-15] DECISION: Premium Sai HyperFrames graphics = a clean recognizable ICON on a high-contrast dark "stage" card + crisp markings + soft drop shadows + ONE motion + a camera payoff + heavy restraint — NOT texture. | REASONING: Gray rejected the first Vid 3 pass (textured CSS towers/gears with dense graph-paper window grids) as "cheap/flat"; studying the starred b2v5 airplane-runway render showed its premium feel comes from a clean iconic object + clean stage + restraint, not detail/texture. CSS-rectangle "buildings" cap out at looking like a ruler. | CONTEXT: Drove the Vid 3 rebuild (rocket-on-track flagship, magnifier-on-landscape, contract+pencil, brand-logo tiles). Companion habit adopted: render draft frames and actually LOOK at them before re-showing Gray. See feedback_prefers_3d_motion_graphics.

[2026-06-15] DECISION: To "actually write" a signature with a pen icon, drive BOTH the stroke reveal (style.strokeDashoffset) and the pen position from one GSAP proxy tween's onUpdate using path.getPointAtLength(len) — set the pen tip to the sampled path point each frame. | REASONING: Tweening pen x linearly while revealing the stroke separately leaves the tip off the curve (it hovers). One proxy keeps tip and stroke-tip locked. Also: gsap.set writes strokeDashoffset as inline STYLE, which overrides setAttribute — must update sp.style.strokeDashoffset, not the attribute. | CONTEXT: ig-signing-clients. Deterministic + seekable (same pattern as the airplane counter onUpdate).

[2026-06-15] DECISION: When re-exporting a HyperFrames .mov over an existing file, write to a NEW filename if the old one may be open in Premiere. | REASONING: Overwriting a locked .mov fails the render's final copy step with "EBUSY: resource busy or locked" after a full (wasted) capture+encode. | CONTEXT: Hit on the Rules/Regulations/Red Tape white-label re-export.

[2026-06-15] DECISION: Locked the next-iteration direction for all three Sai production systems from the 3-memo review. SHORTS: rotate a format menu (S/A/B-tier, choose-X-or-Y, reply-to-comment, varied setups), lean saveable/shareable tips, IG-priority; Gray builds format+topics from daily on-set notes (Notion checklist Sai approves morning-of), Sai scripts the words; retention is the ONLY shorts metric. LONG-FORM: keep the raw doc-diary but make it a STORY not a tutorial — map the arc before filming the A-roll, lock title/thumbnail first, one story per video. LINKEDIN: keep as-is + build a reusable image Drive + repurpose to carousels/stories. | REASONING: Sai's headline across all three memos: the systems work but everything looks too samey; fix = variety + front-loading creative decisions. | CONTEXT: Analysis at business/social-media/sai/reviews/2026-06-14-production-system-review.md; living backlog at SYSTEM-BACKLOG.md; 6 memories (project_sai_production_review_2026_06_14 + feedback_sai_*).

[2026-06-15] DECISION: Built the story-arc board (web-apps/story-arc-board/) FOLDER-SCOPED — you point it at ONE specific footage folder and it loads only those clips as draggable arc cards; it never pulls arbitrary clips from the whole library. | REASONING: Gray explicitly does not want random index pulls; the footage index also has no content column, so whole-library pulls would be noise. The SQLite index is used only to enrich filmed_date when the drive is mounted (ffprobe fallback otherwise). | CONTEXT: Python stdlib server + drag-drop lanes + lock-first spine (one story + title + thumbnail) + save/load; all endpoints tested against generated clips, not yet against real D:/Sai footage.

[2026-06-15] DECISION: Batch 3 shorts were built by cross-merging Sai's REAL transcript talking points (mined from the 73 EP2 clips) x Sandcastles proven hooks/formats. Sandcastles is RESEARCH ONLY — it does not author scripts. | REASONING: Keeps every claim grounded in what Sai actually said while wrapping it in formats with evidence of working in the niche (e.g. the contrarian-declarative outlier). | CONTEXT: 12 scripts at business/social-media/sai/scripts/2026-06-15-batch-3.md; editable review HTML (2026-06-15-batch-3-review.html) generated by .build_batch_review.py, with per-script Sandcastles reference links patched in.

[2026-06-15] DECISION: A/B/C trial-reel hooks must differ VISUALLY (shot / location / person / effect / graphic title), not just in the spoken words. Every hook now carries a one-line Visual: spec. | REASONING: verbal-only variants test phrasing, not the hook — the trial reel can't isolate what moves retention if all three look identical. | CONTEXT: Locked in sai-script-style-guide.md (Hook Testing section) + memory feedback_sai_hooks_need_visual_variation; applied across all 12 Batch 3 scripts; also retired the "Stop X" hook that slipped into Script 2.

[2026-06-15] DECISION: In the VS Code extension, custom project subagents (scriptwriter, footage-puller) are NOT exposed to the Agent tool — only built-in types are. Workaround: dispatch a general-purpose agent and load the agent's .md as its full system prompt; route ALL Sai/Gray script work through the scriptwriter this way. | REASONING: subagent_type 'scriptwriter' errors "Agent type not found"; the agent file's frontmatter is well-formed, so it's an environment limitation, not a file bug. Embedding the full agent prompt makes the general-purpose agent behave as the scriptwriter. | CONTEXT: Batch 3 was written via the lightweight "pointed at the file" version; going forward, embed the full scriptwriter.md. Memory: reference_scriptwriter_invocation.

[2026-06-15] DECISION: EP2 long-form clips are duplicated into 07_QUERY_PULLS/EP2-arc-map/ renamed in story-arc order (NN_Beat_CID_slug) with an _EDIT-GUIDE.md manifest, for clip-by-clip pre-edit before Premiere. | REASONING: Gives Gray the "look at it one by one" review Sai's whiteboard method calls for, with story order baked into the filenames; query-pull = temp duplicates, originals untouched. | CONTEXT: 17 clips pulled from 01_ORGANIZED/06-{06,09,10}-26 + "Rest of week" (found on the SSD, C2669-C2686). Arc reasoning in business/social-media/sai/longform/ep2/EP2-ARC-MAP.md.

[2026-06-15] DECISION: Upgraded the batch-review HTML generator (.build_batch_review.py) with 3 collaboration features: (1) "+ Add card" button — create new blank cards (title/format/A·B·C hooks/script/notes) live in the page, included in exports; (2) "💾 Save file" button — bakes the full current state INTO a complete standalone HTML (originals + every edit + comments + added cards), via showSaveFilePicker with download fallback, so the file itself carries full context (not just the diff that "Copy decisions + edits" produces); (3) per-card 💬 comment threads with a "Commenting as: Gray/Sai" selector — Gray comments → Save file → send to Sai → Sai opens, sees everything, comments as Sai → Save → back. Loader uses a savedAt timestamp so the NEWEST file always wins over stale browser localStorage (fixes the return-trip). | REASONING: Gray wanted (a) to add cards for future batches, (b) to save an actual file so Claude has full context, and (c) a way for Sai to review his changes and add his own. | CONTEXT: All in the generator template, applied to FUTURE batches only — batch-3 HTML deliberately NOT regenerated (would need Gray to export current in-browser edits first). Verified end-to-end with Playwright: bake (11 originals + edits + both authors' comments + new card), fresh-open seed, and newer-file-overrides-stale-local all pass. Test artifacts were throwaway (/tmp), repo shows only the generator changed.

[2026-06-15] DECISION: Executed the D:/Sai drive reorg cleanup (plan 2026-06-14) and emptied the `_TO_DELETE/` holding folder. Kept the newest Ep1 documentary render (`Ep 1 Longform Doc Windows Final.mp4`) in `03_DELIVERED/episodes/`; deleted the other 11 Ep1 render drafts + a 53MB broken "Draft 4" partial + screenshot/Higgsfield junk + the stray `token.json` — ~11GB reclaimed (223→234GB free on Footage). Also re-synced the footage index on Mac and created the Mac-side `.env`. | REASONING: The drive already matched the clean 9-folder scheme (root strays + `01_ORGANIZED` clog were drained by the earlier session); only the `_TO_DELETE` final-yes + a stale 53MB partial remained. Gray chose keep-newest-delete-rest; the genuine final was already safely delivered, so the draft pile was pure redundancy. `token.json` on the footage drive is a credential leak (the real one lives gitignored in the project). | CONTEXT: Ep1 is the active BTS doc, so every delete was confirmed against the keeper first. `.env` holds `SAI_LIBRARY_ROOT` + a placeholder Anthropic key (indexing/pull never call the API; only `organize`/Vision would need a real key). Index was last built 2026-06-06 (pre-cleanup) so it referenced moved/deleted paths until this re-sync.

[2026-06-15] DECISION: Shipped footage-organizer v3 Phase 3 — the `batch` command (`cli_index.py batch --num N --from <folder> --map "1:Cxxxx-Cyyyy …"`). It ensures the week, makes `01_ORGANIZED/Batch_NN/Vid_MM/`, moves each mapped clip range in, re-indexes, and reports unmapped/missing clips. Added `batch_num`/`vid_num` index columns, DERIVED from the folder path (`_batch_vid_from_path`, like `_category_from_path`) so a plain `index` re-derives them. tests 24→54 green; verified end-to-end on a temp library with real ffmpeg-generated clips. | REASONING: Batch shorts shoots were the last thing Gray hand-sorted; one command that files by Vid + tags the index kills that. Deriving the tags from folders (not writing them once) keeps folders the single source of truth. | CONTEXT: Schema migration is NON-destructive — `index._migrate` runs `ALTER TABLE ADD COLUMN` if missing (not the rebuild the v3 plan floated), so the live 841-clip DB keeps every row; chose this over schema_version+rebuild to avoid a full re-walk. Gotcha logged: `idx_batch` must be created in `_migrate` AFTER the ALTER, never in `_SCHEMA` (a `CREATE INDEX` on not-yet-added columns crashes migrating an old DB). Phase 4 (stage-transition helpers ACTIVE→DELIVERED→ARCHIVE) is the only v3 work left.

[2026-06-15] DECISION: Shipped footage-organizer v3 Phase 4 — the `promote` command (`cli_index.py promote --item NAME --to delivered|archive`), completing v3. It finds a finished project (file or folder) by exact name in the source stage, infers its format bucket from where it lives, and moves it to `<next-stage>/<format>/<week>/`. `--from` defaults (active→delivered, delivered→archive); `--format`, `--week`, `--no-week` override. tests 54→70 green; verified end-to-end (active→delivered→archive, overwrite + not-found guards fire). | REASONING: Moving finished videos through stages was Gray's other forget-prone step. The real 02/03/04 layout is a genuine mix (format buckets + legacy named folders + week subfolders + loose files), so a rigid `<format>/<week>/<project>` mover would be brittle — a name-find + infer-format + safe-move handles the mess. Pure file ops (never copy/delete/overwrite, ambiguous-name abort) because a misfile here loses real client edits. | CONTEXT: Stages 02/03/04 are NOT in INDEX_SCAN_ROOTS (only 01 + 05), so promote needs no index/ffprobe — pure moves. v3 is now COMPLETE (Phases 1–4). Out of scope / optional future: re-enabling the Vision auto-classifier once manual library sorts clear an accuracy bar.

[2026-06-15] DECISION: Added the `ship` command (footage-organizer v3.1) — post-delivery cleanup. After a video lands in 03_DELIVERED, `ship --video NAME` PLANS two moves (edit project 02→04_ARCHIVE; raw footage 01→05_FOOTAGE_LIBRARY/<video>/<week>), shows them, and moves nothing until confirmed; then re-indexes. Project found by name; footage located by parsing "Batch N Vid M" from the video name (→ Batch_0N/Vid_0M) or `--footage`. tests 70→79; verified end-to-end (cancel path moves nothing; `--yes` executes both + reindex). | REASONING: Gray wanted "video posted → its edit project archives + its footage files into the library" automated. The real 02/03/04 layout is messy (format buckets + legacy folders + loose files), so plan-first + name-match + warn-and-skip-a-missing-half (never guess) is the safe shape — he explicitly chose "show me first." | CONTEXT: `_ship_plan` (pure, returns moves+warnings) is split from `_execute_ship` + `cmd_ship`'s prompt SPECIFICALLY so a planned folder-watcher can reuse the engine HEADLESS for the trigger Gray actually wants: drop/export a file into 03_DELIVERED → auto-build plan → send for approval → execute. Honest constraint surfaced to Gray: Claude isn't a 24/7 daemon, so the drop-trigger needs a small local watcher running while his Mac is on (the footage drive is local — can't run on the VPS), and it must debounce until the Premiere export finishes writing (file size stable) before planning.

[2026-06-15] DECISION: Built the delivered-folder watcher (`python-scripts/footage-organizer/watch_delivered.py`, footage-organizer v3.2) — Gray's chosen trigger: drop/export a video into 03_DELIVERED → Slack approval. Poll-based (no watchdog dep): `_delivered_videos` + `_stable_new_items` (size-stable-across-two-scans debounce) detect a finished NEW export; first run baselines existing files into `.ship-watch-state.json` so it only reacts to new drops. Reuses the ship engine headless: `_ship_plan` → post plan to Slack → `_await_reaction` polls `reactions.get` for ✅/❌ → `_execute_ship` + `_reindex`. Slack via slack_sdk WebClient + SLACK_BOT_TOKEN/SLACK_USER_ID (same pattern as morning-briefing/creator-intel), imported LAZILY so the pure helpers unit-test without creds. tests 79→84 (pure brain covered); Slack handshake is live-only (`--self-test`). | REASONING: Gray picked "Slack message" approval; he already has a slack_sdk bot token, and a bot token can READ his ✅ via reactions.get (no public server / Socket Mode needed) — so the full post→approve→execute loop works by polling. Poll-based beats watchdog (no extra dep, no half-write events, debounce is trivial). | CONTEXT: NEEDS from Gray (in his own terminal, secrets rule): add SLACK_BOT_TOKEN + SLACK_USER_ID to the footage-organizer .env + grant the bot `reactions:read` scope; verify with `--self-test`. ONE watcher per machine (one-scheduler rule), Mac-only (local drive). launchd daemonization intentionally deferred until a clean foreground live-test. Added slack_sdk>=3.27 to requirements.

[2026-06-15] DECISION: Parked the delivered-folder watcher (watch_delivered.py) — keep it built + tested but do NOT connect Slack or daemonize. Use the `ship` command by hand instead. | REASONING: At ~1 short/day the watcher only saves typing one command, still needs Gray's per-video ✅ (the attention isn't automated), and name-matching is fragile (clean only for Batch N Vid M shorts; longform/pod/LinkedIn projects don't title-match) so it'd often hand back a half-plan to fix anyway — a lot of always-on infra (daemon, Slack scope, state file, Mac-on) for a tiny, unreliable payoff. The real win was the `ship` command itself (one reviewed step). | CONTEXT: Claude gave this assessment honestly when Gray asked "good addition or waste?"; Gray chose to leave `ship` and park the watcher. Re-trigger to connect it: if the drive re-clutters because manual `ship` keeps getting forgotten. Watcher code/tests/docs stay in the repo, dormant — zero cost sitting there.

[2026-06-16] DECISION: The Sai long-form on-screen TITLE CARD is the series name + episode number only — "Trendified Diaries, Episode N" — NOT a thematic title. The theme stays the editorial spine for the arc; any thematic title is the YouTube headline (separate from the on-screen card). | REASONING: Gray clarified the only title cards they use are episode-number branding; this reverses the earlier series-template rule ("title = theme only, no episode number, no date"). | CONTEXT: Updated `business/social-media/sai/longform/ep2/_SERIES-TEMPLATE.md` + `EP2-ARC-MAP.md`; the thematic options in `EP2-TITLE-THUMBNAIL.md` now serve the YouTube headline only.

[2026-06-16] DECISION: LinkedIn is OUTSOURCED and Founder Series is RETIRED in the Content OS. LinkedIn = a 3rd-party AI tool interviews Sai + drafts the posts → Gray adds images + reviews → post daily; our `python-scripts/sai-linkedin` tool is superseded for LinkedIn drafting. | REASONING: Gray confirmed the LinkedIn pipeline is now handled by an external AI-interview tool Sai talks to (the "expensive but so easy" tool he loves from the 6/14 review); Founder Series is no longer produced. | CONTEXT: Updated `business/sai-karra/content-os/README.md` (files table, production tiers, pipeline, what-changed) + memory `feedback_sai_linkedin_keep_and_repurpose`. Don't pitch sai-linkedin as the LinkedIn drafter or re-propose Founder Series.

[2026-06-16] DECISION: Each long-form/shorts Content-OS review stage gets a REUSABLE editable-HTML generator — a Python script that parses the stage's `.md` and emits a self-contained, localStorage-saving, exportable review page Sai opens in a browser. | REASONING: Sai reviews far better in a clean interactive page than raw markdown; generating from the `.md` keeps the source of truth in git and the page always matches; one generator per stage is reusable every episode/week. | CONTEXT: `scripts/.build_batch_review.py` (shorts batch — hook-pick/Approve-Swap-Cut/edit/delete/refs), `longform/.build_interview_review.py` (interview Qs — edit/must-have/cut/add, auto-sizing boxes), `longform/.build_arc_review.py` (arc synthesize board — drag clips between beats, delete cards + beats). HTML opens with no server; renders/footage stay off git.

[2026-06-15] DECISION: Footage library keeps the 17 AI categories as-is and seeds NO starter freeform folders ("just the 17 for now"); Organizer v3 Phase 2 makes the index treat ANY folder under 05_FOOTAGE_LIBRARY/01_ORGANIZED as its category (not just the 17), and ensure_week now discovers on-disk freeform folders + skips `_`-prefixed helpers (e.g. `_TO_SORT`). | REASONING: Gray wants to add his own folders over time without re-sorting the 154G already filed; inventing a parallel taxonomy would confuse manual sorting (the tool's reliability bar). Vision auto-classifier stays paused. | CONTEXT: `python-scripts/footage-organizer/cli_index.py` (_category_from_path + ensure_week), tests 34/34; folder CLAUDE.md + `plans/2026-06-03-footage-organizer-v3.md` updated.

[2026-06-15] DECISION: Disk reorg — `All Broll/` is Sai's OWN footage → `06_ASSETS` (not the reusable library); drained `01_ORGANIZED` non-EP2 shoots → `05_FOOTAGE_LIBRARY/_TO_SORT/` for Gray to hand-sort; nothing is ever hard-deleted — junk + draft renders stage to a `_TO_DELETE/` holding folder for Gray's final yes before emptying. | REASONING: All Broll is footage Sai filmed himself, not Gray's Sai shoots, so it's an asset not library b-roll; the holding-folder pattern keeps an irreplaceable-footage drive safe from silent loss. | CONTEXT: `plans/2026-06-14-footage-reorg-cleanup.md` (EXECUTED); ~11GB reclaimed from Ep1 draft renders (kept `Ep 1 Longform Doc Windows Final.mp4`).

[2026-06-18] DECISION: Gray is pivoting ALL focus to building a NEW version of the Footage Organizer (FO). | REASONING: He wants batch-filtering in place before resuming other batch work — the Batch 3 shorts auto-edit pipeline is gated on FO being able to filter/serve a specific batch of footage. | CONTEXT: Current FO = v3 COMPLETE + ship (v3.1) per context/priorities.md row 8. Batch 3 shorts editing is paused until the new FO batch-filtering is ready; the auto-edit training lessons are logged at business/social-media/sai/editing/shorts-auto-edit-training.md.

[2026-06-19] DECISION: Footage-organizer intake + batch-original archiving reworked. (1) The raw-drop folder is now a DEDICATED inbox `01_ORGANIZED/_INBOX/<date>/` (was the bare `01_ORGANIZED/<date>/`, which shared a parent with the categorized `<category>/<date>/` output and was easy to confuse). `main.py` organize defaults there, `--setup` creates it, and `batch` move-and-clears it (removes the `--from` folder once empty). (2) Batch interview ORIGINALS get their own permanent home `05_FOOTAGE_LIBRARY/_BATCHES/Batch_NN/Vid_MM/` — same library, separate filing system (by batch/vid, NO week), kept OUT of the b-roll search index. `ship` now routes auto-detected batch footage there (loose `--footage` shoots still use the `<category>/<week>` b-roll scheme). (3) The index walker (`_walk_videos`) now skips ALL `_`-prefixed helper folders (`_INBOX`, `_TO_SORT`, `_BATCHES`) so un-categorized/archived footage never enters the searchable index. tests 84→87 green; verified end-to-end on a sandbox library (setup→batch→inbox-clear→ship-plan-to-_BATCHES). | REASONING: Gray's call — the footage library is "mainly for b-roll"; batch interview takes are a different beast and should run on "an entirely different filing system" inside the same library. He wanted a clear order of operations where originals move exactly ONCE (at ship), AI edits never touch them (they read→write to 08_AI_EDITS), and the inbox visibly empties once footage is filed. | CONTEXT: Order of operations now documented in the FO README ("a batch video, start to finish" table) + folder CLAUDE.md. Index lifecycle is intentional: batch originals are searchable (batch_num/vid_num tags) while in 01_ORGANIZED during production, then drop out of the index once shipped into the `_`-prefixed _BATCHES. This is part of the active FOv4 push (batch-filtering); the still-to-build piece is `pull --batch N` to SERVE a filed batch.

[2026-06-19] DECISION: AI-edited outputs are now filed by CONTENT FORMAT inside 08_AI_EDITS — `08_AI_EDITS/shorts/<source>/` for batch / short-form edits and `08_AI_EDITS/longform/<source>/` for long-form / episode edits (was `08_AI_EDITS/<pipeline>/<source>/`). Added `AI_EDIT_FORMAT_BUCKETS = ["shorts","longform"]` to config; `--setup` now creates both buckets. Batch 3 deliverables move from `08_AI_EDITS/Batch_03/...` to `08_AI_EDITS/shorts/Batch_03/...`. Docs synced: FO config/main/README/CLAUDE.md, workflows/footage-organizer{,-onboarding}.md, workflows/batch3-multicam-short-pipeline.md, and the batch3 memory. tests still 87 green; sandbox --setup confirmed both buckets created. | REASONING: Gray wants AI edits grouped by what they ARE (shorts vs longform) so the edit outputs mirror how he thinks about content, not by which pipeline produced them. | CONTEXT: 08_AI_EDITS is a documented convention (the AI-editing pipelines write to it; the footage-organizer tool doesn't route into it beyond creating the buckets at setup), so this is a scheme + docs change, not new routing logic. "longform" here == the "episodes" content used in the 02/03/04/05 format buckets — Gray's word for AI edits is "longform". Old dated one-off scripts with hardcoded historical AI-Edits paths were left untouched (not the convention enforcer).
